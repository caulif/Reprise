# Reprise 工程开发规范整改复审报告

- 复审日期：2026-08-23
- 复审对象：依据《工程开发规范全面审查报告-2026-08-23.md》完成的当前工作树整改
- 复审方式：检查当前 Git 工作树、治理文件、Issue/PR 模板、门禁脚本、CI、发布/安全文档、源码规模，并执行一次完整 `npm run check`
- 结论：自动门禁相关判据在当前工作树上已有可复核证据。函数 100 行上限与 provenance / 正式发布仍未完成，见下文剩余问题。未创建 commit，未做正式 npm 发布，未跑真实 Runtime smoke。

## 整改进度（复审后）

- **P0-1（已落地）**：`verify:pack` 增加 `needs: ['build']`；`checkRequiredPackedPaths` 要求 `package.json` / `README.md` / `LICENSE` / `bin.reprise` 目标 / 至少一个 `dist/src/` 文件；空壳三文件包与缺 bin 包在 `--self-test` 失败；`run-gates --self-test` 断言该依赖。`node scripts/verify-pack.mjs --self-test` 与 `npm run verify:docs` 已通过。
- **P1-1（已落地）**：2026-08-23 及之后的 decision 若不像五节模板会失败；自检增加错误标题、缺状态行、缺全部小节。两份同日旧格式记录已改成模板。`npm run verify:docs` 通过。
- **P1-2（已落地）**：删除 test lane 对 `.tmp-jscpd` 的空 artifact 上传。不再声称测试失败会上传有效日志。
- **P1-3（已落地）**：`docs/CONTRIBUTING.md`、`docs/SUPPORT.md`、`docs/engineering-gates.md` 改为 Windows 11 是产品支持平台，Ubuntu 是 CI 可移植性门禁但不承诺真实 Runtime/TUI 帧。
- **P2-3（已落地）**：原审查报告第 7 节将 artifact、超长函数、provenance、发布演练改回 `[~]` / `[ ]`，并写明未完成证据。
- **P1-4（已落地）**：`verify-audit.mjs` 用 `spawnSync` 始终解析 JSON，只按 `productionAuditFailed` 决定退出码；self-test 覆盖“子进程非零 + moderate JSON”。
- **P1-5（已落地）**：secret 扫描覆盖 tracked/staged/未忽略未跟踪文本和 `test/`；增加 project/Anthropic/fine-grained/bearer 规则；只输出路径与规则名。fixture 中的合成 token 改为运行时拼接。
- **P2-1（已落地）**：Windows 上 `npm` 经 `node npm-cli.js` 且 `shell: false`；self-test 覆盖带空格参数与含空格路径的 `.cmd` shim（`ComSpec /c`）。
- **P2-2（已落地）**：分层 import 识别 `from`、`export ... from`、`import()` 与 side-effect import，各有反向自检。
- **P0-2（已落地文件阈值）**：四个原超限文件均低于 1000 行：`experiment-recovery-run.ts`（985）、`tui/controller.ts`（992）、`recovery-tools.ts`（564）、`local-workspace-provider.ts`（665）。`experiment-recovery.ts` 为再导出（7 行）。`recoverCodexExperiment`、`completeRecoveryReview`、`completeHostCheckpointRecovery`、`CodexIntakeTui` 仍超 100 行，已收窄为例外清单并截止 2026-09-06。`npx tsc --noEmit` 已通过。
- **P2-4（已暂存）**：被引用的新文件已全部进入 index；`git diff --cached --check` 无空白错误。未创建 commit（按用户规则不主动提交）。
- **第 7 节第 10 条（已演练）**：临时目录打出 `reprise-0.1.0.tgz`（109 文件，含 `dist/src/cli/main.js`）；安装该 tarball 后 CLI 为 `reprise 0.1.0 (Node.js 24.11.1)`；用安装包内 `ExperimentStore` 写入 `schemaVersion` 1 事件、把 `events.jsonl` 改为 `schemaVersion` 999 后 `open` 失败（`Event checksum mismatch at sequence 1.`）、从备份写回后原文不变。本仓库无 git tag、无本 harness 的上一发布版。npmjs 上的 `reprise@1.0.0`–`1.1.0` 是无关同名包，未当作上一版本安装。清单命令 `npm publish --access public --dry-run` 因该同名包的 `latest=1.1.0` 高于 `0.1.0` 而失败；`npm publish --access public --dry-run --tag reprise-harness-0.1.0 --ignore-scripts` 成功（`+ reprise@0.1.0`，未实际上传）。未做 provenance 证明。

## 1. 总体评价

本轮整改已经把原报告的大部分“缺少入口”问题落到了仓库中，方向正确，且没有引入大型框架或新依赖：

- 已增加贡献、安全、治理、支持、行为准则和 Changelog 文档；
- 已增加 CODEOWNERS、Bug/Feature/Task Issue 模板和 PR 模板；
- 已增加任务 brief、发布清单和事故复盘模板；
- 已给 gate 失败输出补充命令、退出码/信号、Node、OS 和 Git SHA；
- 已增加 pack allowlist、production audit、secret 扫描和分层 import 检查；
- `src/application/experiment.ts` 已从约 3044 行降到约 673 行；
- 本次复审新运行的 `npm run check` 最终为 15 个 gate 全部通过，421/424 个测试通过、3 个按环境跳过。

这些改动使项目从“依赖维护者经验”明显向“仓库内可执行规则”前进。第 2–4 节记录的缺口已按本报告进度落地。自动门禁与发布 dry-run 可复验；函数 100 行上限、正式发布与 provenance 仍未完成，见第 7 节未勾选项。

## 2. 必须优先修正的问题

### P0-1：`verify:pack` 与 `build` 并行，完整 gate 可能在 `dist/` 被删除时检查出一个不完整包仍判定通过

**证据：**

- `scripts/run-gates.mjs` 中 `verify:pack` 没有 `needs: ['build']`；`check` 和 `static` 会把它与 `build` 同一批并行运行。
- `build` 的第一步会递归删除 `dist/`，之后才运行 TypeScript 编译。
- 本次 `npm run check` 的实际输出是 `verify-pack: ok (3 files)`；完整 build 结束后单独执行 `npm pack --dry-run --json`，包中实际有大量 `dist/src/*.js`，不仅 3 个文件。
- `scripts/verify-pack.mjs` 只检查“出现的路径是否在 allowlist”，没有检查必须存在的 `dist/src/cli/main.js`、`package.json` bin 目标或至少一个 `dist/src` 文件。因此只有 LICENSE、README、package.json 的空壳包也会通过。

**风险：** 发布门禁可能验证的是 build 删除 `dist` 与重新生成之间的瞬间状态；一个没有 CLI 的坏包仍显示绿色。这直接削弱发布供应链 gate 的可信度。

**最小修复：**

1. 给 `verify:pack` 增加 `needs: ['build']`；
2. `checkPackedPaths` 之外增加 required paths 校验，至少要求：
   - `package.json`；
   - `README.md`；
   - `LICENSE`；
   - `dist/src/cli/main.js`，且与 `package.json.bin.reprise` 一致；
   - 至少一个 `dist/src/` 文件；
3. 增加反向 self-test：输入只有三个元数据文件时必须失败；bin 指向不存在文件时必须失败；
4. CI 的 static lane 保持 build → pack 的显式依赖，不依赖执行速度。

**Done-means：** 一次 `npm run check` 中 `verify-pack` 只能在 build 完成后启动；日志中的文件数与随后单独 `npm pack --dry-run --json` 一致；删除 `dist/src/cli/main.js` 的测试 fixture 能使 gate 失败。

**复审后状态：已落地。** `scripts/run-gates.mjs` 中 `verify:pack` 现为 `needs: ['build']`。`checkRequiredPackedPaths` 拒绝空壳包与缺 bin 包；`run-gates --self-test` 覆盖依赖断言。完整 `npm run check` 文件数一致性在本轮全部项收尾时再核一次。

### P0-2：超长文件问题没有实质完成，最大职责只是被整体移动到 2381 行的新文件

**证据：**

当前源码热点为（物理行数，2026-08-24 工作树）：

| 文件 | 约行数 |
|---|---:|
| `src/tui/controller.ts` | 992 |
| `src/application/experiment-recovery-run.ts` | 985 |
| `src/infrastructure/recovery-workspace-tools.ts` | 902 |
| `src/application/experiment-recovery-support.ts` | 849 |
| `src/environment/local-workspace-provider.ts` | 665 |
| `src/infrastructure/recovery-tools.ts` | 564 |
| `src/application/experiment-recovery-review.ts` | 427 |
| `src/application/experiment-recovery.ts` | 7（再导出） |

`docs/decisions/accepted/2026-08-23-oversized-source-file-exceptions.md` 将例外从两个超 1000 行的文件收窄为四个超 100 行的函数/类，截止日期仍为 2026-09-06。

**风险：** Agent 上下文、review、冲突和错误定位成本没有按职责明显下降；1440 行函数仍同时承担过多状态、错误和持久化路径。

**最小修复顺序：**

1. 先拆 `recoverCodexExperiment`，按现有概念抽出 preflight、candidate materialization、forensics/readiness loop、promotion/persistence 四段；只移动已有逻辑，不设计新框架；
2. 再拆 `CodexIntakeTui` 的页面状态与输入路由，优先复用已有 `controller-input.ts`、`controller-run.ts`、`pages/`；
3. `recoveryObservationTools` 按工具注册/校验/执行分离，但不要为每个工具建一个文件；
4. `LocalWorkspaceProvider` 最后处理，先把纯路径/复制/清理 helper 与状态持有分开；
5. 每次只做一个职责切片，先 build，再运行直接受影响的 `dist/test`，最终该轮代码再跑一次 `npm run check`。

**Done-means：** 四个例外文件低于 1000 行；所有函数低于 100 行，或剩余函数拥有比当前更窄、带截止日期的例外；行为测试不因纯移动而弱化。

**复审后状态：文件阈值已落地。** 四个原超限文件均低于 1000 行。`recoverCodexExperiment`、`completeRecoveryReview`、`completeHostCheckpointRecovery`、`CodexIntakeTui` 仍超 100 行，已写入比文件例外更窄的函数/类例外，截止日期 2026-09-06。`npx tsc --noEmit` 通过。

## 3. 中优先级问题

### P1-1：`verify-docs` 对新 decision 的结构校验存在可绕过分支

`scripts/verify-docs.mjs` 的当前逻辑是：

1. 2026-08-23 之前的 decision 直接按祖父规则返回；
2. 对 2026-08-23 及之后的文件，先计算 `usesCurrentTemplate`；
3. 如果不是当前模板，也直接 `return errors`；
4. 只有“已经长得像正确模板”的文件才继续校验状态、小节顺序、备选方案和 accepted 标题。

这意味着一个文件名合法、但正文完全不符合模板的新 decision，可能绕过正文校验。self-test 只覆盖“看起来像模板但缺备选方案”的情况，未覆盖“完全不像模板”的情况。

**建议修复：** 祖父规则可以保留，但新文件 `!usesCurrentTemplate` 必须追加明确错误而不是返回；增加三项反向测试：错误标题、缺状态行、缺全部必需小节。

**复审后状态：已落地。** 新文件不像模板会失败。自检覆盖错误标题、缺状态行、缺全部小节。两份同日旧格式记录已改为五节模板。

### P1-2：CI 的 test 失败 artifact 实际无效，报告中的“已提供脱敏 artifact”属于过度声明

`.github/workflows/check.yml` 的 test lane 只运行 build/test/check:node，失败后上传 `.tmp-jscpd`；但该 lane 不运行 jscpd，因此通常没有这个目录，并且配置了 `if-no-files-found: ignore`。这不能提供测试失败日志、测试报告或环境摘要。

原报告第 297 行把“失败 artifact”标为完成，依据不足。

**建议修复：**

- 最懒的方案是删除这个无效 upload 步骤，避免虚假保障；
- 如果确实需要 artifact，让 test 命令同时 tee 到 `.tmp-test-output.txt`，失败时上传该文件和脱敏环境摘要；
- 不上传 `.reprise/`、完整 prompt、用户路径、模型输出或凭据；
- 为 artifact 路径存在性增加最小 workflow/static self-test，或将 `if-no-files-found` 改为 `error` 以阻止静默空上传。

**复审后状态：已落地。** 已删除 test lane 的 `.tmp-jscpd` 上传。test 失败不再声称有 artifact。audit lane 仍上传 `docs/tui-audit/`（该 lane 确实生成该目录）。

### P1-3：平台支持文档与 CI 合并政策互相矛盾

`docs/CONTRIBUTING.md` 声明：Windows 11 是唯一已验证环境，“其他 OS 上的失败可报告，但不构成回归门禁”。但 `.github/workflows/check.yml` 的 test matrix 包含 `ubuntu-latest`，最终 `all-checks-passed` 要求整个 test job 成功，所以 Ubuntu 失败会阻止合并，事实上已经构成回归门禁。

**建议二选一：**

- 推荐：把文档改成“Windows 11 是产品支持平台；Ubuntu 是 CI 可移植性门禁但不承诺真实 Runtime/TUI 帧支持”；
- 或者把 Ubuntu 改为真正的非阻断诊断 lane，但这会降低现有跨平台保护，不建议。

**复审后状态：已落地。** 文档改为：Windows 11 是产品支持平台；Ubuntu 是 CI 可移植性门禁，不承诺真实 Runtime/TUI 帧。

### P1-4：production audit 对 moderate 漏洞的声明与实际异常路径不一致

`productionAuditFailed` 和 self-test 宣称“仅 moderate 时通过”；但 `execFileSync` 遇到 npm audit 非零退出会进入 catch。catch 只有在 high/critical 时自行退出，否则最终重新抛出原异常。因此，只要 npm 对 moderate 报告返回非零，实际 gate 仍会失败，而不是按函数策略通过。

**建议修复：** 使用 `spawnSync` 或捕获 `execFileSync` 的 stdout 后始终解析 JSON，再仅依据 `productionAuditFailed(report)` 设置退出码；把“subprocess 非零 + moderate JSON”加入端到端 self-test，而不只是测试纯函数。

**复审后状态：已落地。** `spawnSync` 始终解析 stdout JSON；仅 `productionAuditFailed` 为 true 时失败。self-test 覆盖子进程非零 + moderate JSON。

### P1-5：secret gate 覆盖不足，且本地检查会忽略未跟踪文件

当前 `verify-secrets.mjs`：

- 只读取 `git ls-files`，本地尚未 `git add` 的新文件不参与扫描；本轮新增的多个未跟踪脚本在本次本地 gate 中就没有被扫描；
- 整个 `test/` 被跳过，真实 secret 若误放进 fixture 不会被发现；
- 模式只覆盖传统 `sk-`、`ghp_`、Slack token 和部分 private key；未覆盖项目实际可能使用的 OpenAI project key、Anthropic key、GitHub fine-grained token、通用 Authorization/Api-Key 高熵值等；
- 只检查少数文本扩展名，shell、dotenv 示例或其他配置类型可能漏掉。

**建议修复：**

1. 扫描 tracked + staged + 未跟踪但未被 `.gitignore` 忽略的文本文件；
2. 不要整体跳过 `test/`，只对自检的合成 token 用精确行级 allowlist/占位拼接；
3. 增加项目真实凭据类型的模式，并优先考虑成熟的 GitHub secret scanning/gitleaks 作为 CI 层；本地脚本保留为廉价补充；
4. 输出文件和规则名，不输出匹配到的 secret 值；
5. 反向测试包含未跟踪文件、test fixture 和新式 token 格式。

**复审后状态：已落地。** 扫描 tracked/staged/未忽略未跟踪文本，含 `test/`；规则含 project/Anthropic/fine-grained/bearer；输出只有路径与规则名。

## 4. 可以继续优化但不应阻塞当前整改的项目

### P2-1：Windows gate 启动方式触发 Node 安全弃用警告

本次 `npm run check` 一开始出现 DEP0190：在 `shell: true` 时向 child process 传 args 会产生未转义拼接风险。当前 gate 命令是仓库常量，直接利用风险较低，但告警会污染日志，未来若参数来自输入会扩大风险。

**建议：** Windows 下直接执行 `npm.cmd`，使用 `shell: false`；其他 gate 也保持 executable/args 分离。self-test 要覆盖带空格路径和 `.cmd` shim，符合项目 Windows 优先规则。

**复审后状态：已落地。** `npm` 经 `node npm-cli.js` 且 `shell: false`。self-test 覆盖带空格参数，以及含空格路径的 `.cmd`（`ComSpec /c`）。编排路径不再对 npm 使用 `shell: true`。

### P2-2：分层 import gate 是正则近似，不是完整 TypeScript 依赖分析

当前脚本只匹配 `from "./..."`，可能漏掉动态 `import()`、side-effect import、`export ... from` 和某些路径别名。项目已有 `product-contract.test.ts` 覆盖 core/Pack 的重要边界，因此现阶段不必引入新解析器。

**建议：** 先扩展现有正则覆盖 `import "..."`、`export ... from` 和 `import()` 三种真实用法，并给每种一个反向 fixture；只有出现实际漏检后再考虑复用 TypeScript compiler API。

**复审后状态：已落地。** 三种语法均有反向自检。

### P2-3：整改报告自身状态互相矛盾，应改为事实记录而不是勾选宣传

根报告顶部写了最新整改进度，但第 304–316 行仍保留旧审查的“未运行 npm run check”说明；第 301 行把“写了清单、未真实发布”表述成“已经演练”；第 299 行把“清单中提到 registry 支持时开启”当成 provenance 已有明确结果；第 297 行把空 artifact upload 当成已完成。

**建议：** 不继续维护旧报告的勾选状态。旧报告保持当时事实，新建本复审报告记录当前状态；长期事实分别进入 decision、release checklist 和 CI。若一定要更新旧报告，至少将这些项改回 `[ ]` 或 `[~]`，并写明未完成证据。

### P2-4：当前工作树尚未形成可安全提交的单一范围

`git status` 同时存在 staged、unstaged、删除和未跟踪文件；关键新增文件包括 gate 脚本、decision 和 `src/application/experiment-recovery.ts`。如果只提交当前 staged 内容，`experiment.ts` 的拆分可能缺少未跟踪实现文件，CI 会直接失败。

**建议：** 提交前执行：

```text
git status --short
git diff --cached --stat
git diff --stat
git diff --check
```

把整改按可独立验证的范围拆为治理文档、门禁、Recovery 纯移动/拆分等提交；不要把删除大量历史 analysis、治理入口、供应链 gate 和 2000 行代码移动混成一个不可审查提交。不要主动提交 `.env`、`.reprise/`、日志或真实 smoke 产物。

## 5. 已经做得比较到位的部分

以下部分无需为了“更完整”继续加流程：

1. **治理入口位置可接受。** 文件位于 `docs/` 而不是根目录，但根 README 和 docs README 已提供入口，GitHub 模板也指向正确位置；没必要再复制一份到根目录。
2. **Issue/PR 模板足够轻。** 必填字段覆盖问题、影响、证据、范围、Done-means、验证和回滚，没有引入复杂审批层级。
3. **CODEOWNERS 足够简单。** 当前单维护者阶段用全局 owner 加高风险目录即可，不需要虚构 backup owner。
4. **Gate 诊断输出改善有效。** 非零退出和 spawn 失败都有 self-test；本次完整 gate 能输出清晰汇总。
5. **Actions 固定 SHA、最小权限、并发取消和超时设置合理。** 暂时不需要额外 CI 框架。
6. **真实 Runtime smoke 继续显式 opt-in 是正确边界。** 本次复审没有运行，避免外部费用和凭据风险。
7. **超长文件使用限期例外比机械拆分更安全。** 问题是尚未完成，不是例外机制本身错误；应按截止日期逐步拆，而非现在一次性重写。

## 6. 建议整改顺序

### 第一批：立即修，预计 0.5–1 天

1. 修复 `verify:pack` 的 build 依赖和 required files 反向用例；
2. 修复新 decision 可绕过模板校验；
3. 删除或修正 test lane 的空 artifact upload；
4. 修正文档中的 Ubuntu 门禁表述；
5. 将原报告的 provenance、发布演练、artifact 完成项改为未完成或部分完成。

### 第二批：1–3 天

1. 修复 audit 非零退出的 JSON 解析路径；
2. 扩展 secret 扫描范围，取消整个 `test/` 豁免；
3. Windows gate 改用 `npm.cmd` + `shell: false`；
4. 整理 staged/unstaged/untracked 范围，形成可审查提交。

### 第三批：在 2026-09-06 例外到期前

1. 按职责拆 `experiment-recovery.ts`；
2. 拆 `tui/controller.ts`；
3. 拆 `recovery-tools.ts`；
4. 最后拆 `local-workspace-provider.ts`；
5. 每次只运行直接受影响测试，最终每批代码改动只跑一次完整 `npm run check`。

## 7. 建议的最终 Done-means

- [x] `verify:pack` 明确依赖 build，空壳包和缺 bin 包都能被反向用例拒绝；
- [x] 任意 2026-08-23 之后、文件名合法但正文畸形的 decision 会使 `verify:docs` 失败；
- [x] test lane 失败时要么上传真实脱敏日志，要么不声称有 artifact；
- [x] Windows 产品支持与 Ubuntu CI 可移植性门禁的文档表述一致；
- [x] moderate-only audit 的实际子进程路径与声明策略一致；
- [x] secret gate 能检查未跟踪待提交文本和 test fixture，且不打印 secret；
- [x] 完整 gate 不再出现 DEP0190；
- [x] 四个超限文件低于 1000 行；主要超长函数收窄为带 2026-09-06 截止日期的函数/类例外；
- [x] Git 暂存范围包含所有被引用的新文件，`git diff --cached --check` 无错误；
- [x] 发布 dry-run、当前 tarball 安装、数据读取回滚已各演练一次。本 harness 无上一发布版可装。正式发布与 provenance 仍未做。

## 8. 本次复审验证证据

已执行：

```text
npm run check
# 含 prepublishOnly：15 通过，0 失败；421 pass，3 skipped；约 74.2 秒

git diff --cached --check
# 无空白错误

npm pack --pack-destination %TEMP%\reprise-release-drill-20260824
# reprise-0.1.0.tgz；entryCount 109；含 LICENSE、README.md、package.json、dist/src/cli/main.js

npm install <tarball> --omit=dev
# exit 0；node node_modules/reprise/dist/src/cli/main.js --version → reprise 0.1.0 (Node.js 24.11.1)

# 安装包内 ExperimentStore：写入 schemaVersion 1；将 events.jsonl 改为 schemaVersion 999 后
# open 失败（Event checksum mismatch at sequence 1.）；从备份写回后可读，原文与备份一致

npm view reprise versions
# 1.0.0 1.0.1 1.0.2 1.0.3 1.1.0（无关同名包）；git tag 为空

npm publish --access public --dry-run
# 失败：Cannot implicitly apply the "latest" tag … previously published version 1.1.0

npm publish --access public --dry-run --tag reprise-harness-0.1.0 --ignore-scripts
# 成功 dry-run：109 files，+ reprise@0.1.0；未实际上传
```

未执行：

- 真实 Codex/Claude Runtime smoke：没有 opt-in，避免外部费用；
- npm 正式发布与 provenance 证明：dry-run 不产生 attestation；同名占用也阻止按默认 `latest` 发布 0.1.0；
- 安装本 harness 的上一版本：仓库无 tag、registry 无本包 0.x；
- 对用户真实历史目录的破坏性或权限测试：不属于本次复审边界。

## 9. 最终结论

自动门禁与第 8 节发布 dry-run 可在当前工作树复验。超过 100 行的函数/类未清零，以 `scripts/source-size-allowlist.json` 与截止日期 2026-09-06 为准。正式发布与 provenance 仍未做。未创建 commit。
