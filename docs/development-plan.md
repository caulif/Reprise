# Reprise 开发计划

当前唯一活跃的实现顺序、边界和验收见 [Session / harness / workflow 架构重构规划](./plan/reprise-architecture-redesign.md)。该规划按执行机制与持久化、harness、Controller、Comparison、Workflow/TUI、平台与 Pack、文档收口七批推进。

本文件保留为工程入口兼容路径，不重复旧的模块 0–8 计划。每一批实现必须同步当前规范、决策记录和测试证据；完成的专题计划迁入 `docs/.local/`，不能继续作为实现依据。
