# 本次运行全流程复盘与提分建议

> 复盘对象：`recovery-b0f8eaee-f70a-4fca-a125-72949131733e` / `run-f4ddb43a-fa7c-4caa-a8c7-01f41f2d4f11`  
> 复盘日期：2026-09-04

## 结论先行

这次失败不是“用户只要 HTML、模型只会 HTML”。真实会话是连续对话：先要求 HTML，随后确认白底和统一风格，再明确要求“新建一个白底的 PPT，把 HTML 的三页内容复刻上去”。

Controller 的核心问题不是“没有阶段状态机”，而是**没有正确利用已经提供的原始会话来判断当前候选是否完成**。它实际上读取了 `message-350`（明确的 HTML→PPT 要求），但仍然只发送了 HTML 样式修正，最后在第四次决策直接 `done/satisfied`。这是 Controller Agent 的判断和提示词设计问题，不能归因于用户没有提供上下文。

隔离副本在 run 结束时自动释放，则是独立的系统生命周期问题，直接造成已生成的 HTML 后来不可访问。这个行为不符合本任务的可审阅要求。

## 1. 原始会话表达了什么

| 原始消息 | 语义 |
|---|---|
| `message-6` | 根据 Excel 和参考 PPT，先制作 2–3 页 PPT 风格 HTML |
| `message-155` | HTML 改为白底，页面风格统一 |
| `message-216` | 按同样风格继续制作可信代码库内容 |
| `message-350` | 明确要求“新建一个白底的 PPT”，复刻 `可信代码库成果展示.html` 三页内容 |
| `message-541` | 要求说明每页数据源 |

所以，“HTML → PPT”不是我事后推断出的流程，而是原始用户消息中的明确动作。

## 2. 证据链

本次 Controller 在第二轮确实读取了：

- `history/transcript/message-155.txt`
- `history/transcript/message-216.txt`
- `history/transcript/message-350.txt`
- `history/transcript/message-541.txt`

对应事件见：

```text
events.jsonl sequence 192–200
```

但是它随后生成的决策仍然是：

> 请继续完善白底、统一版式和数据源标注，修改后覆盖 HTML。

它没有发送“根据已确认 HTML 新建 PPT”的消息。

最后一次决策是：

> 已核对最新 HTML……数据源已修正……done/satisfied。

这说明问题不是“历史会话未落盘”或“Controller 完全没读到”，而是**读到了却没有把明确的后续交付要求纳入完成判断**。

## 3. 这次产生了什么

文件级事实：

```text
.pptx / .ppt / .png：0 个
changedPaths：能力提升计划-漏洞挖掘情况汇报.html
runtimeGeneratedPaths：[]
```

候选最终消息也只声称 HTML 已生成，并把 PDF/PNG 导出留给用户手工完成。

HTML 曾位于隔离副本：

```text
...\\environment\\runs\\run-f4ddb43a-fa7c-4caa-a8c7-01f41f2d4f11\\能力提升计划-漏洞挖掘情况汇报.html
```

run 结束后隔离副本被释放，系统只保留变更路径、文本快照和追踪 artifact，没有保留可直接打开的 HTML/PPT 交付物。

## 4. 为什么 Controller 会做出错误判断

### 4.1 提示词把“历史用户消息”定位成了接受习惯，而不是未完成任务来源

Controller 提示词写了：

- “History is for whether this user would stop or steer, not a queue to send in order.”
- “Do not fire historical user sentences in sequence.”
- “after_first_deliverable=1 means that user line came after a first visible assistant deliverable.”

这些规则本意是防止机械重放历史对话，但副作用是：Controller 容易把 `message-350` 当成“历史轨迹中的一个后续偏好”，而不是当前任务仍未完成的明确交付要求。

对于本次会话，正确判断应是：

- `message-155` 是对 HTML 的修改意见；
- `message-216` 是继续扩展内容；
- `message-350` 改变了交付格式并提出新的明确动作；
- `message-541` 是对最终交付的追溯要求。

Controller 应综合这些消息判断“用户是否还会继续”，而不是只在当前 HTML 上做局部修正。

### 4.2 Controller 读取了消息，但缺少“动作提取 + 当前产物对照”

现有实现提供了 `historicalUserFollowups` 和 transcript 文件，但没有形成一个清晰的判断步骤：

1. 从后续用户消息中提取明确动作、目标格式和对象；
2. 对照当前 workspace 中实际产物；
3. 判断哪些动作已经满足，哪些仍未满足；
4. 只有全部满足才允许 `done`。

因此模型虽然读到了 `message-350`，但没有完成“要求—产物”对照，直接被当前轮 HTML 的局部质量吸引。

这属于 Agent 的判断失败，也暴露出提示词没有把最重要的推理任务说清楚。

### 4.3 `done/satisfied` 的证据门槛过低

当前 `done` 是一个自由语义决定，Host 只验证 schema 和 evidence ref，不验证：

- 用户明确提到的 PPT 是否存在；
- HTML 是否已经被转换/复刻；
- 目标文件是否可打开；
- 用户最后要求的路径是否可见；
- 当前结果是否只是中间稿。

所以 Controller 可以在“HTML 看起来不错”的情况下结束，而不需要证明“PPT 已完成”。

这里不应通过系统硬编码 HTML/PPT 阶段来替代 Agent 判断；但必须让 Agent 的完成判断具备足够的输入、检查方法和推理约束。

## 5. 系统问题：隔离副本释放

隔离副本释放与 Controller 判断是两个问题：

- Controller 错误地提前结束；
- 即使它正确结束，用户也需要继续打开 HTML、PPT 和截图；
- 当前 `CandidateRun.#cleanup()` 在捕获有限 artifact 后调用 `environment.release`；
- 该设计把“Runtime 停止”错误地等同于“用户不再需要 workspace”。

本任务要求 workspace 至少保留到用户完成审阅，或由用户显式触发清理。保留 workspace 是交付生命周期不变量，不是 HTML/PPT 阶段的硬编码。

## 6. 不应采用的修复思路

不建议把产品改成写死的：

- HTML 草稿阶段；
- HTML 确认阶段；
- PPT 阶段；
- 最终交付阶段。

这些可以作为诊断报告的分析语言，但不应成为系统判断规则。否则系统会从“Agent 没有理解会话”滑向“系统替 Agent 编排固定流程”，失去对自然语言任务的泛化能力。

## 7. 应采用的 Agent-first 修复

### 7.1 改 Controller 输入，而不是替它做阶段机

在每次 steering 请求中，Controller 应获得一个清晰的历史会话摘要，至少包含：

- 初始目标；
- 首次交付后的每条用户消息；
- 每条消息中的动作、对象、格式和约束；
- 当前候选已生成的文件及其路径；
- 当前轮之后仍未对应到产物的明确要求。

这不是替 Agent 做决定，而是让 Agent 真正能基于完整上下文做决定。

### 7.2 在提示词中加入“要求—产物对照”职责

应明确告诉 Controller：

> 读取全部首次交付后的用户消息。不要按顺序机械重放，但必须提取其中仍然有效的明确动作。把每个动作与当前 workspace 的实际文件和当前轮结果逐一对照。只要存在一个明确要求尚未满足，就发送下一条自然用户消息；不能因为当前 HTML 修改完成就结束。

并明确要求它识别：

- “新建一个 PPT”是格式变化；
- “把 HTML 内容复刻上去”是转换动作；
- “告诉我每页数据源”是交付追溯要求；
- “白底、统一风格”是约束，而不是最终交付本身。

### 7.3 保持 Agent 判断，增加最小 Host 事实反馈

Host 不判断语义上的“该不该做 PPT”，但应向 Controller 提供客观事实：

- 当前 changed paths；
- 当前候选目录实际存在的文件；
- 产物扩展名；
- 可打开/可渲染检查结果；
- 持久化交付物路径。

这样 Controller 可以自己推理，不需要 Host 写死流程。

### 7.4 `done` 仍由 Agent 决定，但必须基于可验证事实

Host 可以拒绝“引用不存在文件”的结论，但不应替 Agent 决定业务阶段。正确边界是：

- Agent：判断用户意图、是否继续、下一条该说什么；
- Host：提供事实、保存 workspace、验证路径和 artifact ownership；
- 用户：最终审阅和显式清理。

## 8. 结果页和 workspace 的最低修复

结果页应显示：

```text
Run: <完整路径>
Workspace: <完整路径>
Deliverables: <完整路径>
HTML: <完整路径>
PPTX: <完整路径或未生成>
Screenshots: <完整路径或未生成>
```

run 结束时：

- 停止 Runtime；
- 保存所有候选文件到 experiment-owned deliverables；
- 保留隔离 workspace；
- 仅在用户显式清理后释放。

## 9. 最终责任结论

1. **直接流程判断错误：Controller Agent。** 它读取了 `message-350`，却没有把“新建 PPT 并复刻 HTML”作为未完成要求继续发送。
2. **根本设计缺陷：Controller 的历史会话利用策略。** “不要机械重放历史”被实现/解释成了“不要把后续明确动作纳入当前完成判断”。
3. **交付丢失的系统缺陷：workspace 生命周期。** run 结束自动释放隔离副本，导致 HTML 无法继续审阅。
4. **候选模型是次要因素。** 它没有主动把 HTML 转成 PPT，但如果 Controller 正确引导，仍有机会完成下一轮。

这次不应通过硬编码固定阶段解决，而应通过更完整的历史上下文、明确的 Agent-first 对照职责、客观产物事实和可持续 workspace 来修复。

## 10. 下一次运行的 Done-means

- Controller 能在读取原始会话后识别并发出 HTML→PPT 的下一步消息；
- 只有在 Agent 判断“没有未完成的明确用户动作”后才允许 `done`；
- HTML、PPTX、PNG 和 workspace 在用户审阅前均可访问；
- 结果页显示完整路径；
- 若某格式未生成，结果必须明确写“未生成”，不能以 HTML 完成替代 PPT 完成；
- `npm run verify:docs` 通过。

