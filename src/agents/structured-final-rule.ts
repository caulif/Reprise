/** Shared first sentence of structured OUTPUT_CONTRACT texts. */

export const STRUCTURED_FINAL_RULE =
  "The last assistant message contains exactly one JSON object: no Markdown fence, no explanation, no process sentence. Repair turns call no tools; they only correct the JSON.";
