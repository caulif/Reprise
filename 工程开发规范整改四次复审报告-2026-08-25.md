# 工程开发规范整改四次复审报告（2026-08-25）

## 一、结论摘要

本轮整改已经解决上一轮最严重的可复现性问题：TUI 实现文件目前受 Git 控制，`verify-tracked-source` 已接入 `check`/`static`，source-size 不再把整个类误判为函数，ESLint/Knip 不再直接忽略这些 TUI 文件。当前本地门禁整体通过，但还不能称为“所有工程不足均已消除”。

本轮仍发现：

- **P1：源文件规模门禁只扫描 `src/`，没有覆盖 `test/`，而仓库中有 3 个测试源文件超过 1000 行。** 这与 `AGENTS.md` 的“源代码文件不超过 1000 行”存在口径缺口。
- **P1：`tsconfig.json` 与 `jscpd` 仍保留指向已删除 TUI 文件的排除规则，未来可重新引入被门禁隐藏的源码。**
- **P1：`verify-tracked-source` 通过正则读取 ESLint 配置，双引号、计算表达式或多段配置可绕过其 ignore 覆盖检查。** 当前配置没有触发绕过，但门禁本身不够稳健。
- **P2：TUI 拆分后的 `intake-tui-methods.ts` 仍有 804 行、约 50 个委托函数，结构可维护性一般；本次没有发现行为回归，但应继续收敛模块职责。**
- **P2：本地验证仍有 3 个 Windows symlink 相关测试跳过；未取得全新 checkout 和 GitHub Actions 证据。** 这不是本地失败，但发布前仍需补齐证据。
- **P2：staged diff 仍报告两个文件末尾多余空行。** 应在提交前清理。

因此建议结论写为：**核心整改已基本到位，门禁与 Git 可复现性明显改善；尚有 3 项门禁完整性问题和若干质量/证据问题，不建议标记为“全部完成”。**

## 二、发现项与可执行修复

### P1-1：source-size 没有检查测试源文件

**证据：**

- `scripts/verify-source-size.mjs:9` 将扫描根目录固定为 `src`，`main()` 只调用 `scanDirectory(SRC, ...)`。
- 当前统计结果：
  - `test/codex-experiment.test.ts`：2251 行；
  - `test/codex-intake.test.ts`：1434 行；
  - `test/codex-pack.test.ts`：1078 行。
- 当前 `npm run check` 仍会通过，因为这 3 个文件从未进入 source-size 扫描。

**风险：**

门禁文档写的是“文件超过 1000 行失败”，而实际实现只约束生产 `src/`。如果“源代码文件”包含测试代码，当前绿灯会给出不完整结论；即使项目决定测试不受此规则约束，也没有在决策文档中明确写出这个边界。

**最小修复建议（二选一，必须形成明确决策）：**

1. 推荐：将扫描根目录扩展为 `src/`、`test/`，必要时另行决定 `scripts/`；拆分上述 3 个测试文件，并为 1001 行测试文件增加反向用例。
2. 如果确实只约束生产源码：把门禁、决策记录和 `AGENTS.md` 改成明确的“仅 `src/**/*.ts`”，并说明测试文件的规模治理方式，避免文档与实现继续含糊。

不要仅把这 3 个测试文件加入 allowlist；它们是持续维护负担，不是短期例外。

### P1-2：TypeScript 与重复代码门禁仍有已删除路径排除项

**证据：**

- `tsconfig.json:21` 仍排除：
  - `src/tui/controller-ops.ts`
  - `src/tui/controller.ts.*.ts`
  - `src/tui/controller-state.ts`
- 当前这些路径均不存在。
- `package.json:51` 的 `jscpd --ignore` 仍包含 `src/tui/controller-ops.ts` 和 `src/tui/controller.ts.*.ts`。

**风险：**

这些规则看起来是上一次 TUI 拆分遗留物。若有人以后重新创建匹配路径，TypeScript 构建和重复代码检查会静默跳过它们，形成“源码被添加但不受门禁控制”的回归入口。它也会让维护者误以为这些文件仍是生成物或已获批准例外。

**最小修复建议：**

- 从 `tsconfig.json` 和 `package.json` 删除不存在路径的排除项；
- 如果确有必要保留某个例外，改为带 owner、原因、到期日的单独决策记录，并新增反向测试：临时创建匹配文件后，门禁必须失败，而不是被忽略；
- 在 `verify-tracked-source` 或独立配置门禁中检查 `tsconfig.exclude`、Knip/jscpd ignore 不得覆盖受控 `src/**/*.ts`，避免同类回归。

### P1-3：ESLint ignore 覆盖检查依赖脆弱的文本正则

**证据：**

- `scripts/verify-tracked-source.mjs:67-71` 通过正则读取 `eslint.config.js`，只匹配 `ignores: [...]` 且只提取单引号字符串。
- `scripts/verify-tracked-source.mjs:45-60` 自行实现 glob 匹配，而不是读取 ESLint 实际解析后的配置。
- 反向用例 `scripts/verify-tracked-source.mjs:90-96` 只覆盖单引号的精确路径，没有覆盖双引号、`src/**`、`src/**/*.ts`、多段 flat config 或变量展开。

**风险：**

将 `ignores: ['src/tui/**']` 改写为 `ignores: ["src/tui/**"]`，或者把 ignore 数组拆成变量/多个配置块，可能使这个门禁漏报；而 ESLint 仍会真正忽略文件。门禁的“不得通过 ignore 隐藏受控源码”因此不是语法无关的保证。

**最小修复建议：**

- 优先使用 ESLint 的实际配置解析能力，对每个受控 `src/`/`test/` 文件调用 ESLint 配置 API，判断该文件是否被 ignore；不要解析 JS 文本。
- 如果暂时保留自解析器，至少支持单/双引号、跨行数组、多个 `ignores` 配置块，并新增双引号和 `src/**/*.ts` 反向用例。
- 对 glob 匹配使用已有 glob 库或 ESLint 同源实现，不维护一个不完整的自制 glob 解释器。

### P2-1：TUI 拆分后的方法模块仍偏大，且引入大量“委托壳”

**证据：**

- `src/tui/intake-tui-methods.ts` 为 804 行，并导出大量 `CodexIntakeTui_*` 函数。
- `src/tui/intake-tui.ts:179-约246` 为同名实例方法逐一 `.call(this, ...)` 的委托层。

**评价：**

这次拆分解决了文件超限、Git 跟踪和 class 误判问题，且本地行为测试通过；但它主要是机械搬移，不等于职责边界已经清晰。方法仍共享一个巨大的可变 TUI 对象，后续修改容易产生隐式状态耦合。

**可执行优化：**

- 不要再次通过 prototype/interface merge、公开 `__field` 或扩大 ESLint/Knip ignore 来规避门禁；
- 按职责把方法逐步收敛为少量模块：配置、会话发现/分页、历史、运行/结果、渲染与导航；
- 每次只移动一个职责簇，并保留一个现有 TUI 行为测试作为回归检查；
- 当共享状态确实需要减少时，优先传入窄的 state/依赖对象，而不是再新增一层通用抽象。

这项属于后续可维护性优化，不应为了“看起来更短”而牺牲当前行为。

### P2-2：staged diff 存在末尾多余空行

**证据：**

`git diff --cached --check` 报告：

- `src/tui/intake-tui-methods.ts:804`：new blank line at EOF；
- `工程开发规范整改三次复审报告-2026-08-25.md:215`：new blank line at EOF。

**修复：** 删除文件末尾多余空行，保留一个标准换行符，然后重新执行 `git diff --cached --check`。这不是运行时问题，但会污染提交质量检查。

### P2-3：验证证据仍不是干净检出与双平台证据

**本轮已取得的证据：**

- `npm run check`：17 个 gate 通过，0 失败，0 跳过；
- 测试：436 个，433 通过、0 失败、3 个跳过；
- `node scripts/verify-tracked-source.mjs --self-test`：通过；
- `node scripts/verify-source-size.mjs --self-test`：通过；
- `verify-source-size`：0 个登记超限、0 个未登记、0 个过期例外；
- `git diff --cached --check`：发现上述两个 EOF 空行；
- 未运行真实 Runtime smoke，未产生外部费用。

**证据边界：**

- 当前 check 在 Windows 工作区完成，不能替代 Ubuntu CI 的 `static`/`test` lane；
- 3 个跳过项均是 Windows symlink 能力不可用，不应写成“全部测试完整执行”；
- 尚未基于最终 staged 内容做一次独立 clean checkout 的 `npm ci` + `npm run check`；
- 真实 Codex/Claude Runtime smoke 依然按项目规则未执行，不能宣称外部运行时兼容性已验证。

**建议：**

1. 清理 EOF 空行后，使用临时目录从最终提交内容创建 clean checkout；
2. 在该目录执行 `npm ci`、`npm run check`；
3. 在 GitHub Actions 的 Ubuntu/Windows lane 都成功后，再把整改状态写成“验证完成”；
4. 真实 Runtime 仅在明确 opt-in、确认费用和凭据边界后单独执行。

## 三、已确认整改到位的部分

1. `src/tui/intake-tui.ts`、`src/tui/intake-tui-state.ts`、`src/tui/intake-tui-methods.ts` 当前均受 Git 控制，未发现 ignored TypeScript。
2. `verify-tracked-source` 已加入 `scripts/run-gates.mjs` 的 `check` 与 `static` 模式，并有 self-test。
3. source-size 已按文件以及函数/方法 AST 跨度检查，不再将整个 class 作为函数；self-test 覆盖 1001 行文件、101 行函数/方法、宽类和过期例外。
4. 本轮没有新增第三方依赖，package 版本未出现漂移。
5. `npm run check` 当前没有发现编译、lint、测试、pack、secret、layer import、TUI frame 或生成文档失败。

## 四、建议执行顺序

1. 先删除 `tsconfig.json`/`package.json` 中已删除 TUI 路径的排除项，或为确需保留的例外补充反向测试。
2. 明确 source-size 的扫描边界；推荐纳入 `test/` 并拆分 3 个超长测试文件。
3. 将 ESLint ignore 检查改为基于 ESLint 实际解析结果，并补双引号/多配置反向测试。
4. 清理两个 EOF 空行。
5. 运行 clean checkout + `npm ci` + `npm run check`，再核对 Ubuntu/Windows CI。
6. 最后再做 TUI 职责收敛，避免一次性大规模重构。

## 五、本轮复审使用的命令

```powershell
npm run check
node scripts/verify-tracked-source.mjs --self-test
node scripts/verify-source-size.mjs --self-test
git diff --cached --check
git diff --cached --stat
git diff --cached --name-status
Get-ChildItem src,test,scripts -Recurse -File -Include *.ts,*.mjs
```

本报告只记录审查结果，不执行提交或推送。

## 六、按本报告整改后的核验

- **P1-1（已改）**：`verify-source-size` 扫描 `src/` 与 `test/`。测试文件只受 1000 行文件上限约束，不按 100 行函数阈值失败；已拆分 `codex-experiment` / `codex-intake` / `codex-pack` 超长测试。自检拒绝 1001 行 `test/*.test.ts`。
- **P1-2（已改）**：已从 `tsconfig.json` 与 `jscpd --ignore` 删除不存在的 TUI 路径。`verify-tracked-source` 拒绝 `tsconfig.exclude` 覆盖 `src/`/`test/`，以及指向已删除受控源码的 jscpd/knip ignore。
- **P1-3（已改）**：ESLint ignore 覆盖检查改为 `ESLint.isPathIgnored`；自检覆盖双引号精确路径、`src/**/*.ts` 与多段 `ignores`。
- **P2-1（已改）**：`intake-tui-methods.ts` 改为按职责再导出 lifecycle / config / history / sessions / nav 模块，不再堆一个 800 行方法文件。
- **P2-2（已改）**：工作区文件末尾保持单一换行；以 `git diff --check` 为准。
- **P2-3（本地干净检出已改）**：最终 index 内容抽出到临时目录后执行 `npm ci` + `npm run check`，17 通过、0 失败、0 跳过。3 个测试跳过仍是 Windows symlink 能力。Ubuntu/Windows GitHub Actions 与真实 Runtime smoke 仍不在本次整改范围内。