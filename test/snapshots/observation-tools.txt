[
  {
    "name": "read_observation",
    "description": "Read a bounded page of the frozen TaskCase transcript (historical session, not this candidate) or this candidate run event trace.",
    "parameters": {
      "type": "object",
      "required": [
        "source"
      ],
      "properties": {
        "source": {
          "anyOf": [
            {
              "const": "run_events",
              "type": "string"
            },
            {
              "const": "transcript",
              "type": "string"
            }
          ]
        },
        "start": {
          "minimum": 0,
          "type": "integer"
        },
        "maxItems": {
          "minimum": 1,
          "maximum": 128,
          "type": "integer"
        }
      }
    }
  }
]
