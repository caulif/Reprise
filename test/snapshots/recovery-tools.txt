[
  {
    "name": "ls",
    "description": "List a bounded directory within staging. Paths must be relative.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "maxLength": 512,
          "type": "string"
        },
        "depth": {
          "minimum": 0,
          "maximum": 4,
          "type": "integer"
        }
      }
    }
  },
  {
    "name": "read",
    "description": "Read a bounded byte range from a regular staging file.",
    "parameters": {
      "type": "object",
      "required": [
        "path"
      ],
      "properties": {
        "path": {
          "minLength": 1,
          "maxLength": 512,
          "type": "string"
        },
        "offset": {
          "minimum": 0,
          "type": "integer"
        },
        "maxBytes": {
          "minimum": 1,
          "maximum": 262144,
          "type": "integer"
        }
      }
    }
  },
  {
    "name": "grep",
    "description": "Search file contents in staging; returns a bounded list of path:line matches.",
    "parameters": {
      "type": "object",
      "required": [
        "query"
      ],
      "properties": {
        "query": {
          "minLength": 1,
          "maxLength": 256,
          "type": "string"
        },
        "path": {
          "maxLength": 512,
          "type": "string"
        }
      }
    }
  },
  {
    "name": "find",
    "description": "Find staging paths whose names contain a bounded substring.",
    "parameters": {
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "minLength": 1,
          "maxLength": 256,
          "type": "string"
        },
        "path": {
          "maxLength": 512,
          "type": "string"
        }
      }
    }
  },
  {
    "name": "edit",
    "description": "Replace one exact text span in an existing staging file.",
    "parameters": {
      "type": "object",
      "required": [
        "path",
        "oldText",
        "newText"
      ],
      "properties": {
        "path": {
          "minLength": 1,
          "maxLength": 512,
          "type": "string"
        },
        "oldText": {
          "minLength": 1,
          "maxLength": 262144,
          "type": "string"
        },
        "newText": {
          "maxLength": 262144,
          "type": "string"
        }
      }
    }
  },
  {
    "name": "write",
    "description": "Create or overwrite one explicit regular file in staging. recovery.md is the report sink.",
    "parameters": {
      "type": "object",
      "required": [
        "path",
        "content"
      ],
      "properties": {
        "path": {
          "minLength": 1,
          "maxLength": 512,
          "type": "string"
        },
        "content": {
          "maxLength": 262144,
          "type": "string"
        }
      }
    }
  },
  {
    "name": "powershell",
    "description": "Run one PowerShell command with cwd locked to staging. Network is open; credentials and global configuration are not provided.",
    "parameters": {
      "type": "object",
      "required": [
        "command"
      ],
      "properties": {
        "command": {
          "minLength": 1,
          "maxLength": 32768,
          "type": "string"
        }
      }
    }
  }
]
