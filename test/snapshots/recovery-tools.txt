[
  {
    "name": "ls",
    "description": "List a directory; the result is capped. Omit path or use workspace/ for the writable copy; source/ is the read-only user directory when mounted.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "maxLength": 512,
          "type": "string"
        },
        "depth": {
          "minimum": 0,
          "maximum": 4,
          "type": "integer"
        }
      }
    }
  },
  {
    "name": "read",
    "description": "Read a byte range of a regular file, or return the whole file as a native image block when format=image is authorized.",
    "parameters": {
      "type": "object",
      "required": [
        "path"
      ],
      "properties": {
        "path": {
          "minLength": 1,
          "maxLength": 512,
          "type": "string"
        },
        "offset": {
          "minimum": 0,
          "type": "integer"
        },
        "maxBytes": {
          "minimum": 1,
          "maximum": 262144,
          "type": "integer"
        },
        "format": {
          "anyOf": [
            {
              "const": "text",
              "type": "string"
            },
            {
              "const": "image",
              "type": "string"
            }
          ]
        },
        "mimeType": {
          "pattern": "^image/[A-Za-z0-9.+-]+$",
          "type": "string"
        }
      }
    }
  },
  {
    "name": "grep",
    "description": "Search file contents; returns a capped list of path:line matches. Prefix source/ to search the read-only user directory.",
    "parameters": {
      "type": "object",
      "required": [
        "query"
      ],
      "properties": {
        "query": {
          "minLength": 1,
          "maxLength": 256,
          "type": "string"
        },
        "path": {
          "maxLength": 512,
          "type": "string"
        }
      }
    }
  },
  {
    "name": "find",
    "description": "Find paths whose names contain a substring; the result is capped. Prefix source/ to search the read-only user directory.",
    "parameters": {
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "minLength": 1,
          "maxLength": 256,
          "type": "string"
        },
        "path": {
          "maxLength": 512,
          "type": "string"
        }
      }
    }
  },
  {
    "name": "edit",
    "description": "Replace one exact text span in an existing file. Only writable paths are accepted.",
    "parameters": {
      "type": "object",
      "required": [
        "path",
        "oldText",
        "newText"
      ],
      "properties": {
        "path": {
          "minLength": 1,
          "maxLength": 512,
          "type": "string"
        },
        "oldText": {
          "minLength": 1,
          "maxLength": 262144,
          "type": "string"
        },
        "newText": {
          "maxLength": 262144,
          "type": "string"
        }
      }
    }
  },
  {
    "name": "write",
    "description": "Create or overwrite one regular file. Only writable paths are accepted.",
    "parameters": {
      "type": "object",
      "required": [
        "path",
        "content"
      ],
      "properties": {
        "path": {
          "minLength": 1,
          "maxLength": 512,
          "type": "string"
        },
        "content": {
          "maxLength": 262144,
          "type": "string"
        }
      }
    }
  },
  {
    "name": "shell_exec",
    "description": "Run one shell command with cwd fixed to the writable copy; the Host selects PowerShell or a POSIX shell. For read-only source access use the REPRISE_SOURCE_MOUNT environment variable; source/ is a file-tool virtual prefix, not a directory under the work copy. Keep source inspection and work-copy mutation in separate shell calls. Network is open; credentials and global configuration are not provided. The command text is checked for sensitive file names. Read-only mounts cannot be written.",
    "parameters": {
      "type": "object",
      "required": [
        "command"
      ],
      "properties": {
        "command": {
          "minLength": 1,
          "maxLength": 32768,
          "type": "string"
        }
      }
    }
  }
]
