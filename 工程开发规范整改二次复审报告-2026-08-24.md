# Reprise 工程开发规范整改二次复审报告

- 复审日期：2026-08-24
- 复审范围：当前工作树中已暂存的工程治理、CI/门禁、文档与 Recovery/TUI 拆分改动
- 复审方式：规范符合性、CI 完整性、核心路径、边界与安全、简化性复核；TypeScript AST 函数跨度扫描；完整本地门禁
- 总体结论：上一轮 P0/P1 问题大部分已正确修复。P1-1、P1-2、P2-1、P2-2 已落地。`recoveryTools` 与 `recoverCodexExperiment` 主流程均已拆到 100 行以下并移出 allowlist。未创建 commit，未跑真实 Runtime smoke。

## 整改进度

### P1-1（已修复）

`scripts/verify-secrets.mjs` 对工作区 tracked/untracked 文本用 `readFileSync` 扫描，对 `git diff --cached --name-only --diff-filter=ACMR` 的路径额外 `git show :<path>` 读 index blob。finding 形如 `path (index|working-tree)  [rule]`，不打印匹配值。`--self-test` 在临时 git 仓库中构造「index 为合成 token、工作区为安全文本」并断言失败；同时注入 `scanSecretSources`。`node scripts/verify-secrets.mjs --self-test` 与 `npm run verify:secrets` 均退出 0，自检输出含「index 与 working tree 分歧时，index secret 被拒绝」。决策记录见 `docs/decisions/accepted/2026-08-23-supply-chain-and-layer-gates.md`。

### P1-2（拆分已落地；fail 路径必须 `await`，待完整 `npm run check`）

ADR 与 [`scripts/source-size-allowlist.json`](scripts/source-size-allowlist.json) 为唯一例外登记。`recoveryTools` 拆成 `createRecoveryToolContext`、`decorateRecoveryTools` 与 12 个单工具工厂。`recoverCodexExperiment` 只做 session 编排；主流程按阶段落在 `experiment-recovery-run-preflight.ts`、`experiment-recovery-run-forensics.ts`、`experiment-recovery-run-model.ts`、`experiment-recovery-run-finalize.ts`。`executeRecoverCodexExperiment` 已从 allowlist 删除。`verify:source-size` 反向用例仍拒绝 1001 行文件、101 行函数和过期例外。

拆分后 `catch` 里若 `return failRecoveryRunSession(...)` 而不 `await`，`finally` 会先 `store.close()` 释放 writer，随后 fail 路径 `commitArtifact` 报必须先 acquire lock。现已改为 `return await failRecoveryRunSession(...)`。

### P2-1（已修复）

[`工程开发规范整改复审报告-2026-08-23.md`](工程开发规范整改复审报告-2026-08-23.md) 摘要与第 9 节改为：自动门禁可复验；函数 100 行上限、正式发布与 provenance 未完成。

### P2-2（已审核，删除 11 份过程分析）

下列 staged 删除均为时点分析/计划草稿。长期约束已在 `docs/architecture/`、`docs/decisions/accepted/` 与 `docs/plan/`。

| 删除文件 | 处置 |
|---|---|
| `recovery-module-architecture-analysis.md` | 删除。约束见 Recovery 已接受决策与架构总览。 |
| `recovery-agent-automatic-workspace-restoration-architecture-plan.md` | 删除。隔离候选与受控写入见已接受决策。 |
| `recovery-agent-testing-and-acceptance-plan.md` | 删除。验收见 plan 与 evaluation 决策。 |
| `recovery-current-review-and-next-optimization.md` | 删除。过程材料。 |
| `recovery-offset5-5plus5-analysis-and-optimization.md` | 删除。样本评估见 plan 与 checkpoint 决策。 |
| `recovery-real-runtime-5plus5-assessment-and-minimal-optimization.md` | 删除。同上。 |
| `recovery-real-session-validation-plan.md` | 删除。未闭合项在 `docs/plan/`。 |
| `controller-module-architecture-analysis.md` | 删除。见 [`docs/architecture/controller.md`](docs/architecture/controller.md)。 |
| `controller-agent-capability-evaluation-plan.md` | 删除。见 Controller evaluation lane 决策。 |
| `controller-next-steps-and-real-evaluation.md` | 删除。过程材料。 |
| `controller-real-result-analysis-and-discussion.md` | 删除。过程材料。 |

## 一、优先处理的复审发现

### P1-1：secret gate 只收集了 staged 文件名，却读取工作区内容，没有扫描 index 中真正待提交的字节

**位置**：

- `scripts/verify-secrets.mjs:43-52`
- `scripts/verify-secrets.mjs:94-103`
- `工程开发规范整改复审报告-2026-08-23.md:16,150-167`

**现状**：`listSecretScanPaths` 同时调用 `git ls-files`、`git diff --cached --name-only` 和 `git ls-files --others --exclude-standard`，但返回值只有路径。主循环最终统一使用 `readFileSync(join(ROOT, file))` 读取工作区文件，而不是读取 `git index` 中的 staged blob。

**可复现的漏检场景**：

1. tracked 文件的工作区内容中写入 secret；
2. `git add` 把该 secret 放入 index；
3. 不重新暂存，直接把工作区文件改回无 secret 内容；
4. 当前 gate 会读取无 secret 的工作区版本并通过，但真正待提交的 index 版本仍含 secret。

因此，报告中的“覆盖 tracked/staged”只有“覆盖 staged 路径”成立，“覆盖 staged 内容”并不成立。当前 self-test 也只断言路径被列出，没有构造 index 与工作区内容不同的反向用例。

**最小可执行修复**：

1. 工作区 tracked/untracked 文件继续用 `readFileSync` 扫描；
2. 对 `git diff --cached --name-only --diff-filter=ACMR` 返回的路径，额外通过 `git show :<path>` 或 `git cat-file --batch` 读取 index blob；
3. finding 中标明来源为 `index` 或 `working-tree`，但仍只打印路径与规则名，不打印匹配值；
4. 增加反向用例：index 中为合成 token、工作区为安全文本，gate 必须失败；
5. 按项目规则，修改门禁时同一次变更必须带这个可使门禁失败的自动化用例。

**验收命令**：

```powershell
node scripts/verify-secrets.mjs --self-test
npm run verify:secrets
```

预期：两条命令均退出 0；self-test 明确打印“index 与 working tree 分歧时，index secret 被拒绝”。

### P1-2：文件已降到 1000 行以内，但函数 100 行上限仍大量违反，临时例外清单与实际扫描结果不一致

**位置**：

- `docs/decisions/accepted/2026-08-23-oversized-source-file-exceptions.md:15-20,37-53`
- `src/application/experiment-recovery-run.ts:83-992`
- `src/infrastructure/recovery-workspace-tools.ts:61-593`
- `src/application/experiment-recovery-review.ts:74-443`
- `src/application/experiment-recovery-fail.ts:19-246`
- `src/application/experiment.ts:186-469,486-672`
- `src/infrastructure/recovery-observation-tools.ts:24-139`
- `src/application/experiment-recovery-checkpoint.ts:38-147`

**现状**：本轮确实完成了“单文件低于 1000 行”，这是实质进展；但 AST 扫描仍得到以下主要生产代码热点：

| 符号 | 约跨度 |
|---|---:|
| `recoverCodexExperiment` | 910 行 |
| `recoveryTools` | 533 行 |
| `completeRecoveryReview` | 370 行 |
| `executeExperiment` | 284 行 |
| `failRecoverCodexExperiment` | 228 行 |
| `runControllerLoop` | 187 行 |
| `finishExperiment` | 147 行 |
| `recoveryObservationTools` | 116 行 |
| `completeHostCheckpointRecovery` | 110 行 |
| `runProcess` | 105 行 |
| `Claude sessions.importFromBytes` | 105 行 |
| `Codex text caller #complete` | 101 行 |

此外，`completeRecoveryReview` 内还有约 207 行的嵌套异步回调。ADR 第 15-20 行只列出 4 个函数/类作为例外，但实际超过 100 行的不止这 4 个；第 53 行“超过 100 行的剩余符号仅限本文函数/类清单”与代码不符。`recoveryTools`、`failRecoverCodexExperiment` 和 `recoveryObservationTools` 是本次新拆出的文件，却仍以单个超长工厂/流程函数承载大部分复杂度，说明部分拆分只是把文件边界切开，尚未形成可独立验证的职责边界。

**风险**：

- 910 行主流程仍难以局部审查，取消、持久化、清理和失败分类容易互相影响；
- 例外清单不完整，截止日期无法机械核验；
- 当前 `npm run check` 不检查文件/函数规模，所以门禁全绿不能证明该规范满足。

**最小可执行修复**：

1. 先修正 ADR 清单，使其与自动扫描结果一致；不要把尚未完成写成完成；
2. 优先拆 `recoverCodexExperiment`，按现有阶段抽取纯粹的“输入校验与初始化”“候选物化”“模型尝试循环”“review/持久化收尾”，不引入新抽象层或新依赖；
3. `recoveryTools` 不再维持一个 500 行工厂，按已经存在的 observation/workspace 职责返回工具组，再在入口合并数组；
4. 对 101-147 行的轻微超限函数采用直接提取 helper，避免架构重写；
5. 使用已安装的 `typescript` AST 增加 `verify:source-size`，检查源文件和函数跨度；若保留临时例外，allowlist 必须包含符号、owner、到期日，并在过期后自动失败；
6. 新增门禁时按 ADR 要求附反向用例：构造 1001 行文件和 101 行函数，均必须使门禁失败。

**建议 Done-means**：

```text
node scripts/verify-source-size.mjs --self-test  => 反向用例通过
node scripts/verify-source-size.mjs              => 0 个未登记超限；0 个过期例外
npm run check                                    => 15/16 个门禁全部通过（取决于是否新增 gate）
```

如果维护者决定暂不继续拆分，则至少应把所有现存超限符号准确登记，而不是只登记 4 个，并把“完成”表述改为“文件阈值完成、函数阈值限期整改”。

## 二、非阻塞但建议修正的问题

### P2-1：复审报告摘要仍容易让读者误判为全部完成

`工程开发规范整改复审报告-2026-08-23.md:6` 写“第 7 节 10 条完成判据……均已有可复核证据”，但同一报告第 19 行承认函数仍超 100 行，第 21 行承认未做 provenance，原全面报告也仍把相关项目标为 `[~]` / `[ ]`。详细正文已经比上一版准确，但摘要应直接写明“多数完成，函数规模与 provenance/真实发布仍未完成”，避免只读结论的人误判。

**建议改法**：把结论拆成“已通过的自动门禁”和“未完成/人工项”两句，并链接到剩余问题表。

### P2-2：删除并忽略 `docs/analysis/` 前，应确认长期有效结论已迁移

当前改动删除 11 个 `docs/analysis/*.md` 并在 `.gitignore` 中忽略整个目录。工作区现有文档没有发现对这些文件名的残余链接，文档门禁也通过，因此不会形成直接断链。但这些文件名包含 Recovery 架构、测试验收和真实运行评估；在提交批量删除前，应确认其中长期有效的约束已进入 `docs/architecture/`、`docs/product/` 或 `docs/decisions/`。一次性过程材料可以留在 `.local`，架构原因和失败教训不应只保留在维护者本机。

**建议验收**：为每个被删文档做一次“删除 / 已迁移到稳定文档”的清单；只迁移仍然有效的结论，不恢复整批历史草稿。

## 三、已确认修复到位的项目

1. **pack 顺序已修复**：`verify:pack` 依赖 `build`，不会再与清理/生成 `dist` 并行。
2. **空壳包已被拒绝**：pack gate 要求元数据、真实 bin 目标及 `dist/src/` 内容；本轮实际检查为 109 个文件。
3. **decision 模板绕过已修复**：2026-08-23 及之后的新 decision 不符合标题、状态和五节结构时失败。
4. **audit moderate 路径已统一**：无论 npm 子进程是否非零，先解析 JSON，再只按 high/critical 策略决定失败。
5. **无效 test artifact 已删除**：test lane 不再上传不会生成的 `.tmp-jscpd`。
6. **平台声明已统一**：Windows 11 是产品支持平台；Ubuntu 是阻断合并的 CI 可移植性门禁，但不代表真实 Runtime/TUI 支持。
7. **Windows npm 启动已收敛**：使用 `node npm-cli.js` 与 `shell:false`，本轮未再出现 DEP0190。
8. **分层 import gate 覆盖增强**：覆盖 `from`、`export ... from`、动态 import 和 side-effect import，并有反向自检。
9. **源文件阈值已完成**：当前生产源文件均低于 1000 行；需要继续处理的是函数阈值。
10. **Git 范围已整理**：复审开始时所有既有改动均在 index，未发现 unstaged/untracked 混杂；`git diff --cached --check` 通过。
11. **没有引入新依赖**：本轮治理和拆分复用了 Node.js、Git、npm 与已安装 TypeScript，符合 Ponytail 的最小依赖原则。

## 四、验证证据

### 已运行

```powershell
npm run check
git diff --cached --check
```

结果：

- `npm run check`：16 个 gate 通过，0 失败，0 跳过（含新增 `verify:source-size`）；
- Node tests：424 个，共 421 通过、3 个因当前 Windows symlink 能力不可用而跳过；
- `verify:pack`：通过，包内 109 个文件；
- `verify:docs`、`verify:secrets`、`verify:audit`、`verify:imports`、`verify:source-size`：均通过；
- `git diff --cached --check`：无输出，退出码 0。

secret index/worktree 分歧已有 self-test 反向用例。`recoverCodexExperiment` 与 `recoveryTools` 已按阶段拆到 100 行以下，并从 allowlist 移除。

### 未运行或无法由本地门禁证明

- 未运行真实 Codex/Claude Runtime smoke，避免未显式授权的外部费用；
- 未验证 GitHub Actions 托管环境的实际执行结果；
- 未做正式 npm publish，也未获得 registry provenance。

## 五、建议整改顺序

1. 其余 allowlist 符号按到期日继续拆分。
2. 完成代码/门禁修改后跑 `npm run check`；纯文档修正跑 `npm run verify:docs`。

## 六、最终判断

pack 竞态、decision 绕过、audit 异常路径、无效 artifact、平台表述、Windows shell、import 正则、secret index 扫描与 source-size 门禁均已落地。`recoverCodexExperiment` 与 `recoveryTools` 已拆到 100 行以下。其余函数超限仍依赖限期例外。未创建 commit，未跑真实 Runtime smoke。
