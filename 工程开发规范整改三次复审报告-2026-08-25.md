# Reprise 工程开发规范整改三次复审报告

- 复审日期：2026-08-25
- 复审对象：当前未暂存工作树中的 source-size 清零、TUI 搬移、类/函数拆分及相关门禁配置
- 复审方式：Git 可提交性、干净检出可重现性、门禁完整性、TypeScript 类型边界、Ponytail 简化性和完整本地 `npm run check`
- 总体结论：当前不能认定为“全部改好”。本地 16 个 gate 虽然全绿，但存在 1 个 P0 和 2 个 P1：TUI 的真实实现是被 `.gitignore` 排除的未跟踪文件，当前 diff 提交后会缺源码；source-size gate 又显式跳过该实现；为清空 allowlist 而采用的 prototype/interface merge 还扩大了公共可变状态并关闭类型安全规则。

## 一、阻塞提交的问题

### P0-1：`controller.ts` 依赖两个被忽略、未跟踪的源码文件，当前改动不可从 Git 干净检出构建

**位置**：

- `src/tui/controller.ts:1`
- `.gitignore:81`
- 本地文件 `src/tui/intake-tui.ts`（约 1086 行，未跟踪且被忽略）
- 本地文件 `src/tui/intake-tui-state.ts`（约 110 行，未跟踪且被忽略）

**证据**：

```text
git ls-files src/tui/intake-tui.ts       => 无输出
git ls-files src/tui/intake-tui-state.ts => 无输出
git status --short --ignored -- ...      => 两个文件均为 !!
git check-ignore -v ...                  => .gitignore:81 /src/tui/intake-tui*.ts
```

但当前受控的 `src/tui/controller.ts` 只有一行：

```ts
export { CodexIntakeTui, type CodexIntakeTuiOptions } from "./intake-tui.js";
```

这意味着当前 `npm run check` 使用了维护者机器上被忽略的本地源码，所以会通过；提交现有 diff 后，干净 checkout 没有 `intake-tui.ts` / `intake-tui-state.ts`，TypeScript 无法解析该模块。由当前工作区打出的 `dist` 和 npm tarball也包含 Git 无法复原的代码，破坏发布可重现性和 provenance。

**最小修复**：

1. 删除 `.gitignore` 中 `/src/tui/intake-tui*.ts`；
2. 将实际运行所需的两个文件纳入版本控制；
3. 不要把 1086 行实现作为“生成物”豁免。将其按已有职责移动少量 helper，使每个受控文件低于 1000 行；
4. `controller.ts` 可以继续作为兼容再导出，但其目标必须是受控文件；
5. 增加廉价门禁：禁止 `src/`、`test/` 中存在 ignored TypeScript 文件，或至少验证受控源码的相对 import 目标也受 Git 控制。

**Done-means**：

```powershell
git ls-files -- src/tui/intake-tui.ts src/tui/intake-tui-state.ts
# 预期输出两个路径

git ls-files --others -i --exclude-standard -- "src/**/*.ts" "test/**/*.ts"
# 预期无输出
```

随后必须在干净 checkout 上执行一次 `npm ci` 与 `npm run check`，不能再用当前含 ignored source 的目录作为最终证据。

## 二、门禁被削弱的问题

### P1-1：source-size gate 显式忽略真实 TUI 实现，使“0 个超限”结论失真

**位置**：`scripts/verify-source-size.mjs:105-115,190`

`isIgnoredGeneratedSource` 使用 `/^intake-tui.*\.ts$/` 跳过全部 `intake-tui*.ts`，self-test 甚至断言 `intake-tui.ts` 必须被忽略。但该文件没有 generated/do-not-edit 标记，也没有仓库内生成器；它是 `controller.ts` 直接引用的真实产品源码，并且约 1086 行，已经超过 1000 行限制。

所以当前输出：

```text
verify-source-size: ok (0 个已登记超限；0 个未登记；0 个过期例外)
```

并不代表全部源码满足阈值，只代表“未被正则排除的源码”满足阈值。二次复审报告中“`src/**/*.ts` 文件 ≤1000 行”的表述不成立。

**最小修复**：

- 删除 `intake-tui*.ts` 的忽略正则；
- 只允许忽略能够由受控生成器确定性重建、且带生成标记的文件；
- self-test 应反过来断言真实 `src/**/*.ts` 不得因名称匹配而消失；
- 增加仓库级反向用例：放入超限的 `src/intake-tui-probe.ts` 时 gate 必须失败。

### P1-2：ESLint 和 Knip 通过扩大 ignore 来隐藏新错误，CI 完整性发生回退

**位置**：

- `eslint.config.js:7`
- `knip.json:23-31`

本轮把以下受控文件新增到 ESLint 全局 ignore：

- `src/tui/controller-sessions.ts`
- `src/tui/controller-view.ts`
- `test/product-first-intake.test.ts`
- 以及未跟踪的 `src/tui/intake-tui*.ts`

手工执行：

```powershell
npx eslint --no-ignore src/tui/controller-sessions.ts src/tui/controller-view.ts test/product-first-intake.test.ts src/tui/intake-tui.ts src/tui/intake-tui-state.ts
```

结果为 **127 个 error**，包括大量 `no-unsafe-*`、`no-explicit-any`、unsafe declaration merging 等。默认 `npm run lint` 之所以通过，是因为这些文件没有参与检查。

`knip.json` 又用 `src/tui/controller-*.ts` 和多个宽泛入口把文件当作 entry，会掩盖未使用导出/文件。把真实源码或测试整体加入 ignore/entry 不是修复，只是让门禁不再观察它们。

**最小修复**：

1. 从 ESLint ignore 中删除上述真实源码和测试；
2. 修复类型来源，尤其不要依赖 `any` 形态的 Controller 状态对象；
3. Knip 只登记真实程序入口和确有动态加载证据的入口，不使用 `controller-*` 通配符；
4. 新增配置反向检查：`src/` 和 `test/` 的普通受控文件不得进入 ESLint ignore；
5. 验收时同时执行默认 lint 和针对本轮文件的 `eslint --no-ignore`。

## 三、设计与安全边界问题

### P1-3：source-size gate 把“整个类跨度”当作“函数长度”，诱导出 prototype monkey-patching 和公共内部状态

**位置**：

- `scripts/verify-source-size.mjs:66-74,86-96`
- `src/application/candidate-run.ts:36-67,128-145,385-401`
- `src/products/codex/runtime-port.ts:93-120,233-260,390-400`
- `eslint.config.js:43-53`

项目规则是“函数不超过 100 行”，但 gate 把 `ClassDeclaration` 也按 `functionLimit` 计算。一个由多个短方法组成的 300 行类因此也会失败。为清空 allowlist，本轮把多个原生私有字段 `#field` 改成公共 `__field`，把实例方法改成模块函数，再通过 interface declaration merging 和 `Class.prototype.method = fn` 回挂；同时针对 6 个文件关闭 `@typescript-eslint/no-unsafe-declaration-merging`。

例如 `CandidateRun` 原来的 `#runner/#state/#messages` 现在变成公开可写的 `__runner/__state/__messages`。Runtime client 的进程、pending request、closed 状态也采用相同方式。这样虽然让 AST 中的 class span 变短，却带来：

- 内部状态可被包内外调用者直接篡改；
- 类定义、类型接口和运行时 prototype 赋值分散在三处，review 更困难；
- 忘记一次 prototype 赋值不会由 interface declaration 自动保证；
- 为满足错误的度量而关闭类型安全规则，违反 Ponytail 的“原生能力优先、最小代码”原则；
- 这是公共类型面和 Runtime 状态封装变化，却没有看到相应 decision 说明。

**推荐根因修复**：

1. `verify-source-size` 不要把整个 `ClassDeclaration` 按 100 行函数阈值处理；
2. 继续检查 constructor、method、function、arrow/function expression 是否 ≤100 行；
3. 文件总长度仍由 1000 行门禁约束，因此不会出现无限增长的巨型类文件；
4. 恢复原生 class 方法和 `#private` 字段，删除 interface merge、prototype 回挂和对应 ESLint 关闭项；
5. 对真正超过 100 行的方法按职责抽 helper，而不是把所有方法机械移出 class；
6. 门禁反向用例应覆盖 101 行函数/方法；一个含多个短方法但总计超过 100 行的类不应按“函数超限”失败。

这通常会比当前方案更短、更安全，也更符合仓库实际文字规范。

## 四、报告事实表述问题

### P2-1：二次复审报告把门禁绿灯写成了全部源文件达标

**位置**：`工程开发规范整改二次复审报告-2026-08-24.md:6,14-16,172-196`

报告写明 allowlist 为空、`src/**/*.ts` 均满足阈值、P1/P2 已全部落地。但 `intake-tui.ts` 被 source-size、Git、secret scan、ESLint 一起排除，且当前提交范围无法包含它。这些结论需要撤回。

**建议改为**：

- “当前工作区门禁通过，但证据受 ignored source 污染，不能代表干净 checkout”；
- source-size 状态恢复为未完成；
- 在干净 checkout 的完整 CI 证据产生之前，不再写“全部落地”。

## 五、已确认修复到位的部分

1. secret gate 现在确实分别扫描 working tree 与 index blob；index/worktree 分歧反向用例有效。
2. source-size gate 已有 1001 行文件、101 行函数和过期例外反向用例；问题在扫描范围和 class 定义，不在这些基础用例。
3. Recovery 等部分超长流程已经拆为多个阶段函数，方向正确。
4. 当前工作区的 TypeScript 编译、测试、TUI 帧和 pack 检查均可执行。
5. 本轮没有新增第三方依赖。

## 六、验证证据及其边界

### 已运行

```powershell
node scripts/verify-source-size.mjs
node scripts/verify-secrets.mjs --self-test
npx eslint --no-ignore src/tui/controller-sessions.ts src/tui/controller-view.ts test/product-first-intake.test.ts src/tui/intake-tui.ts src/tui/intake-tui-state.ts
npm run check
git diff --check
git check-ignore -v src/tui/intake-tui.ts src/tui/intake-tui-state.ts
git ls-files --others -i --exclude-standard -- "src/**/*.ts" "test/**/*.ts"
```

结果：

- `npm run check`：16 gate 通过、0 失败、0 跳过，耗时约 77 秒；
- Node tests：436 个，433 通过、3 个因 Windows symlink 能力不可用而跳过；
- `verify-source-size`：报告 0 个超限，但它明确跳过 `intake-tui*.ts`，因此该数字不能作为全仓结论；
- 强制 ESLint：127 个错误；
- ignored TypeScript 扫描：发现 `src/tui/intake-tui.ts`、`src/tui/intake-tui-state.ts`；
- `git diff --check`：退出 0，仅有 LF/CRLF 提示；
- 未运行真实 Runtime smoke，未产生外部费用。

### 为什么 16 gate 全绿仍不能通过复审

本地 build、test 和 pack 都能读取工作区中被忽略的两个源码文件；ESLint、Knip、source-size 和 secret gate又分别通过 ignore/entry/扫描规则绕开了它们。因此这些 gate 在当前机器上彼此一致地验证了一个**Git 无法复原的工作区**，而不是将要提交的仓库状态。

## 七、建议整改顺序

1. 先处理 P0：让所有运行所需源码受 Git 控制，并删除 `src/tui/intake-tui*.ts` 的 ignore。
2. 修正 source-size 语义：检查函数/方法，不把整个 class 当作一个函数。
3. 恢复原生 class 私有状态和方法，删除 prototype/interface merge 与规则关闭。
4. 恢复 ESLint/Knip 对真实源码和测试的覆盖，清理 127 个错误。
5. 修正二次复审报告中的完成声明。
6. 最后在**干净 checkout**执行一次 `npm ci` + `npm run check`，并确认 ignored TypeScript 扫描为空。

## 八、最终判断

本轮确实清理了 allowlist 并完成了若干流程拆分，但目前是通过“忽略真实源码”和“改变 class 统计口径的规避式重构”获得绿灯。P0 会直接导致提交后的干净检出缺文件；P1 则削弱 lint、dead-code 和封装边界。因此当前不应提交，也不应标记为全部完成。先恢复 Git 可重现性和门禁真实性，再评估剩余函数拆分。

## 九、按本报告整改后的核验

- `src/tui/intake-tui.ts`、`intake-tui-state.ts`、`intake-tui-methods.ts` 受 Git 控制；`.gitignore` 不再匹配 `intake-tui*.ts`。
- `git ls-files --others -i --exclude-standard -- src test` 对 `*.ts` 无输出。
- source-size 检查函数与方法，不把整个 class 当函数；自检覆盖 101 行方法、宽类、以及名为 `intake-tui-probe.ts` 的超限文件。
- CandidateRun、ExperimentStore、LocalWorkspaceProvider、AgentSessionHost、两个 Runtime Port 恢复 `#private` 与类方法；已删除 declaration-merging 的 ESLint 关闭项。
- ESLint ignore 与 Knip ignore 不再排除真实 TUI 源码和 `test/product-first-intake.test.ts`。
- 二次复审报告撤回“全部源文件达标 / 全部落地”的完成声明。
- 新增 [`docs/decisions/accepted/2026-08-25-source-size-functions-and-tracked-source.md`](docs/decisions/accepted/2026-08-25-source-size-functions-and-tracked-source.md) 与 `verify-tracked-source` 门禁。
