# Reprise 工程开发规范全面审查报告

- 审查日期：2026-08-23
- 审查对象：当前工作树 `Reprise`（排除 `node_modules/`、`dist/`、`.git/`、`.reprise*/`、`.codex-app-server-schema/` 等生成物或本地运行数据）
- 对照基准：`C:\dsh\开源项目-Agentic开发流程与工程规范.md`，版本 1.0（2026-08-17）
- 报告性质：一次性工程审查与整改路线图，不替代架构规范、决策记录或任务 brief
- 工作树基线：审查开始时 `git status --short` 为空；生成报告后检测到大量并行/外部工作树变更。下文按审查读取到的文件事实写成，不把这些未归属变更视为本次整改；提交或继续开发前必须由维护者单独审查当前 `git diff`。

> 本文件按仓库现有 `docs/AGENTS.md` 的规则放在 `docs/.local/`。该目录是一次性审查材料，不进入版本控制；若需要把结论作为项目长期规范，应将结论提炼到对应的 `architecture/`、`decisions/`、`AGENTS.md` 或 `.github/` 文件，而不是直接把本报告当作权威实现文档。

## 1. 执行摘要

项目已经具备相当强的“可验证核心”：根 `AGENTS.md` 明确了源码、Schema、状态机、事件日志、凭据、真实 Runtime opt-in 和 Windows 优先约束；`package.json` 有 build/typecheck/lint/test/coverage/docs/generated/TUI/重复代码等脚本；`.github/workflows/check.yml` 有静态、双平台测试、TUI audit、覆盖率和汇总门禁；`docs/architecture/`、`docs/decisions/`、`docs/progress/MASTER.md` 也已经形成雏形。`npm run verify:docs` 在本次审查中通过。

但与参考规范相比，工程系统仍明显偏“个人/内部项目的高质量实现”，尚未完整达到可持续开源协作级别。最重要的问题不是再增加抽象，而是补齐治理闭环、降低大文件与流程耦合、让门禁失败可诊断、建立安全/发布/社区入口，并把“目前只验证 Windows 11”转化为明确的支持政策。

### 优先级总览

| 优先级 | 主题 | 结论 | 建议时限 |
|---|---|---|---|
| P0 | 贡献、漏洞、发布治理 | 缺少 `CONTRIBUTING.md`、`SECURITY.md`、`CHANGELOG.md`、PR 模板、CODEOWNERS 等公开入口；外部贡献者无法得到完整、可操作的协作路径 | 立即，1 个小迭代 |
| P0 | 源码可维护性 | 至少 4 个源码文件超过仓库自身规定的 1000 行上限，最大 `src/application/experiment.ts` 约 3044 行；这会放大 agent 上下文、review、回归和冲突成本 | 立即建立拆分计划，随后分批实施 |
| P1 | 需求—变更—证据链 | 有大量 `docs/plan/` 和进度记录，但缺少可复用的 Issue/Task Brief/PR 模板与机器可检查的 Done-means 约束 | 7 天 |
| P1 | CI/门禁 | CI 基础较好，但安全、依赖、打包制品、发布、变更影响选择、失败重试/flake 处理和可观测性还不完整 | 14 天 |
| P1 | 安全与供应链 | 有凭据不落盘/不进 artifact 的规则和 Dependabot，但没有公开漏洞报告政策、依赖审计门禁、锁文件/制品 provenance 和安全修复回归流程 | 14 天 |
| P1 | 发布与回滚 | `prepublishOnly` 只跑 `check`，没有版本政策、CHANGELOG、发布前清单、npm provenance、回滚/事故手册 | 14 天 |
| P2 | 架构与边界 | 架构文档较完整，但模块体量与边界漂移风险需要通过依赖规则、owner 和反向用例持续约束 | 30 天 |
| P2 | 社区运营与简化治理 | 缺少响应时限、路线图/owner 继任、积压治理、季度复杂度审计及规则删除机制 | 30–90 天 |

## 2. 审查证据与现状基线

### 2.1 已有优势

1. 根 `AGENTS.md` 已覆盖参考规范第 4、5、6 节中最关键的本地边界：源码先 build、测试读 `dist/`、外部 JSON/持久化过 `Value.Check`、CandidateRun 状态必须经过 `assertTransition`、模型输入要能由事件日志复原、TUI 只读、真实 Runtime 显式 opt-in、凭据不进入事件/artifact/report。
2. `docs/architecture/` 已按领域拆分出 `overview.md`、持久化/崩溃一致性、产品插件兼容性、运行结果、环境、验证、Controller 和比较报告等主题；这比把设计散落在 README 和代码注释中更接近单一事实源。
3. `scripts/run-gates.mjs` 把 gate 依赖建模为 DAG，并在失败时跳过依赖项；`.github/workflows/check.yml` 采用静态、测试、审计、覆盖率和最终汇总 lane，且取消同一 PR 的旧运行。
4. `package.json` 已锁定 Node 版本范围和依赖版本，CI 使用 `npm ci`；Actions 使用 commit SHA 固定版本，供应链基础控制优于仅写浮动 tag。
5. `docs/documentation-structure.md` 与 `scripts/verify-docs.mjs` 已约束目录、链接、决策记录格式、文档预算和受控边界；本次 `npm run verify:docs` 通过。
6. `.gitignore` 已忽略环境文件、密钥扩展名、`dist/`、本地运行数据和实验产物，且 TUI snapshot 基线有明确例外。

### 2.2 需要注意的基线事实

- `test/` 当前有 42 个 `*.test.ts` 文件；测试脚本明确先编译再运行 `dist/test/**/*.test.js`。
- `src/` 中至少这些文件超过 1000 行：`src/application/experiment.ts`（约 3044）、`src/infrastructure/recovery-tools.ts`（约 1621）、`src/tui/controller.ts`（约 1214）、`src/environment/local-workspace-provider.ts`（约 1014）。此外 `src/core/schema.ts`、Pack runtime、Pi host 等也已接近或超过 600–900 行。
- `docs/plan/` 有多份长期计划；`docs/progress/MASTER.md` 有验证历史和风险记录，但这类历史证据不能替代每个变更的 brief、PR 证据和发布记录。
- 当前没有以下公开治理文件：`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`SECURITY.md`、`GOVERNANCE.md`、`SUPPORT.md`、`CHANGELOG.md`、`.github/CODEOWNERS`、`.github/PULL_REQUEST_TEMPLATE.md`，也没有 `.github/ISSUE_TEMPLATE/`。

## 3. 按参考规范逐节审查

### 3.1 项目立项与治理（参考第 1 节）

**现状判断：部分满足，治理公开化不足。** `docs/product/overview.md` 和 README 能说明产品定位、非目标、local-first 和 Runtime smoke 的边界；但没有发现一套面向外部贡献者的角色、权限、决策、支持和行为政策。

**具体整改：**

- 新增 `CONTRIBUTING.md`：开发环境、分支/提交、Issue 分类、任务 brief、测试选择、文档同步、Windows 运行要求、真实 smoke 的费用与 opt-in 边界。
- 新增 `SECURITY.md`：漏洞报告渠道、不要公开披露 secret、受支持版本、响应时限、修复/回归/公告流程。
- 新增 `CODE_OF_CONDUCT.md`、`SUPPORT.md`、`GOVERNANCE.md`；小项目可以写成短文，不要引入委员会。
- 增加 `CODEOWNERS`，至少覆盖 `src/core/`、`src/products/`、`src/infrastructure/`、`src/tui/`、`.github/`、`docs/architecture/`、`docs/decisions/`。
- 在 `docs/README.md` 增加面向贡献者的入口导航，并确保 README 只做入口，不复制流程正文。

**验收：** 新贡献者只读 README → CONTRIBUTING → 对应子树规则即可完成一次安全的本地变更；安全报告可在不创建公开 Issue 的情况下到达 owner；每个高风险目录至少有一名 CODEOWNER。

### 3.2 架构设计与模块边界（参考第 2 节）

**现状判断：架构事实较丰富，但代码体量显示边界仍有承压点。** `docs/architecture/overview.md`、`product-plugin-compatibility.md`、`persistence-and-crash-consistency.md` 等已经定义主要概念和方向；不过 `experiment.ts`、`recovery-tools.ts`、TUI controller 等大文件表明 application/infrastructure/UI 中可能同时承载编排、持久化、错误恢复和展示适配。

**具体整改：**

- 先为四个超长文件画“当前调用图/职责表”，再按已有领域概念拆分；优先抽出纯函数、端口适配、序列化/报告投影，不新增泛化框架。
- 为依赖方向加一个最小静态检查：`core` 不依赖 `tui`/具体 Pack；`application` 依赖端口而非产品判定；Pack 实现端口；TUI 只消费投影/事件。若现有 lint/knip 无法表达，增加一个小脚本并附反向失败用例。
- 给每个子树增加短 `AGENTS.md`（仅在存在特殊不变量时）：`src/core`、`src/products`、`src/infrastructure`、`src/tui`。不要为没有特殊规则的目录制造文件。
- 明确运行生命周期、取消、超时、重试、幂等和并发 owner；在 `docs/architecture/` 只记录当前不变量，方案比较放 `decisions/`。
- 对 on-disk/event/prompt/tool 变化建立 decision checklist，阻止应用层重新判断产品类型或绕过 Schema/状态机。

**验收：** 依赖反向用例能证明违规时 gate 失败；大文件拆分后每个源文件低于 1000 行或有带日期、owner、删除/复评条件的例外记录；关键不变量各有一个最小失败测试。

### 3.3 需求分析、Issue、Task Brief、Done-means（参考第 3 节）

**现状判断：计划资产多，任务入口标准化不足。** `docs/development-plan.md` 有目标、非目标、模块依赖和验收；`docs/progress/MASTER.md` 有进度和历史证据。但计划文件多，且没有仓库级 Issue/PR 模板来强制问题、范围、证据和回滚字段，容易把 agent 直接带入“按实现方案开工”。

**具体整改：**

- 新增 `.github/ISSUE_TEMPLATE/bug.yml`、`feature.yml`、`task.yml`；Bug 至少要求复现、影响、证据、环境，Feature 要求用户问题、非目标和替代方案。
- 新增 `docs/plan/task-brief-template.md`，固定 `Goal / Context / In scope / Out of scope / Constraints / Options / Done means / Rollback`。
- 在 PR 模板中要求链接 Issue/decision、列出改动文件和风险、验证命令及结果、跳过检查的原因、文档/changelog/回滚影响。
- 为长任务规定唯一稳定入口（例如 `docs/progress/MASTER.md`），计划完成后归档一次性材料，避免多个“下一阶段”文件互相竞争。
- 把 Done-means 写成机械条件：命令、输入 fixture、期望退出码/输出/快照，而不是“完成优化”“验证通过”。

**验收：** 新建 Issue/PR 无法省略必需字段；每个非平凡 PR 能指向一个 brief 或明确说明为何不需要；审查者可从变更反推出目标和证据。

### 3.4 人类与 Coding Agent 上下文（参考第 4 节）

**现状判断：根指令强，渐进式披露仍可加强。** `AGENTS.md` 很有价值，但内容密度高且 `src/` 未见子树指令。参考规范要求 agent 只加载任务所需上下文，并定义输出信任模型、边界和长期记忆。

**具体整改：**

- 保持根 `AGENTS.md` 只放全局硬约束、目录地图、命令、验证和安全边界；将架构细节继续链接到权威文档。
- 增加 `src/AGENTS.md` 作为索引，明确 core/application/infrastructure/products/tui 的依赖方向、变更耦合和 focused checks；不复制架构正文。
- 约定 agent 输出格式：摘要、文件、验证命令/结果、未验证风险、是否涉及协议/持久化/提示词/工具面、是否需要 decision。
- 用 `docs/progress/MASTER.md` 或 issue 记录跨会话事实；禁止把“已实现/待办”复制到多个文档。
- 为真实 Runtime、外部网络、用户目录、删除/迁移和 secret 明确“必须人工 opt-in”边界，并在脚本入口再次校验环境变量。

### 3.5 日常开发、Bug、重构和依赖（参考第 5 节）

**现状判断：本地命令齐全，但维护规则没有完全产品化。** `package.json` 有较多直接脚本，根 AGENTS 已定义“改代码跑 check、源码先 build”。然而没有格式化工具/格式门禁，也没有专门的变更影响选择器；四个超长文件与双 Pack 重复是长期维护成本信号。

**具体整改：**

- 先不引入新 formatter；若团队确实需要统一格式，优先采用现有 ESLint 能覆盖的规则并新增 `lint:fix` 说明，避免为格式问题引入大依赖。
- 把 `npm run check`、`npm run verify:docs`、`npm run build`、`npm run test:only` 的适用场景写进 CONTRIBUTING；避免开发者误把真实 smoke 当普通测试。
- 为依赖升级建立小型模板：为何升级、兼容范围、lockfile 变化、release note、`npm run check` 和受影响 smoke；Dependabot PR 必须经过同一模板。
- 对大文件采取“每次触碰时拆一小块”的策略，禁止一次性架构重写；每次拆分保留一个行为回归测试。
- 对空 catch、子进程、文件系统、外部 JSON 和网络边界做一次专项清单审查；日志包含可诊断上下文但绝不包含 token、API key、完整 prompt 或敏感路径内容。

### 3.6 测试与验证（参考第 6 节）

**现状判断：确定性测试和真实 smoke 意识较好，但测试分层/负向控制/证据报告需要公开化。** 测试从 `dist/` 运行、覆盖率有阈值、TUI 帧审计和 Pack fixture 都是优点。问题在于验证命令与结果主要留在进度/临时日志，且没有显式的 unit/integration/contract/e2e 分层约定，也没有看到针对安全门禁、依赖方向、模板完整性等治理规则的反向测试。

**具体整改：**

- 将测试分为廉价单元、Schema/契约、持久化恢复、Pack 集成、TUI/快照、真实外部 smoke；每层定义是否默认运行、是否收费、是否需要 Windows。
- 每次新增 gate/规则都附一个“故意失败”的 fixture 或 self-test；不要只验证干净树退出 0。
- 对取消、超时、部分写入、损坏 JSON、未知事件、路径边界、权限拒绝、重复恢复、未知模型解析和 secret 泄漏增加负向控制。
- 给失败测试输出统一 artifact：命令、Node/OS、git SHA、fixture 标识、是否使用外部服务；外部 smoke 记录费用与脱敏结果。
- 增加 flake 处理政策：失败先重跑一次只用于诊断，不能自动把第二次通过当作合并通过；记录失败率并设删除/修复 owner。
- 覆盖率阈值继续只升不降，但优先覆盖行为分支和错误路径，不为死代码补测试；与根 AGENTS 的删除原则保持一致。

### 3.7 Code Review、PR 和提交（参考第 7 节）

**现状判断：缺少仓库内可执行模板。** 有 Git 历史和决策记录，但没有 PR 模板、CODEOWNERS、review checklist。对于 agent 生成的变更，这会使“范围/必要性、契约 owner、失败与安全、证据、简化”依赖个人记忆。

**具体整改：**

- 新增 `.github/PULL_REQUEST_TEMPLATE.md`，字段采用参考规范模板：Goal/user impact、Approach/alternatives、Scope、Risk、Verification、Documentation/release、Rollback、Residual risks/skipped checks。
- 新增 `docs/reference/code-review-checklist.md` 或合并到 CONTRIBUTING：先范围与 YAGNI，再契约/owner，再失败/安全，再证据，最后简化与运维。
- 约定 commit 只描述实际变更，禁止用“完成全部优化”类模糊标题；大 PR 拆为可独立验证的提交。
- 对 prompt、工具面、事件和 on-disk 格式变更强制链接 decision 与回归测试；对 generated docs 只改源和生成命令，不手改产物。
- 给 agent PR 增加人工确认项：未信任 agent 的测试/截图/外部 smoke 声明，审阅代码和日志而不是只看报告。

### 3.8 文档工程（参考第 8 节）

**现状判断：这是本项目最成熟的部分之一。** `docs/AGENTS.md`、`documentation-structure.md`、`verify-docs.mjs` 已实现目录职责、链接检查、文档预算、decision 结构和受控边界；本次文档门禁通过。

**具体优化：**

- 清理或标记长期膨胀的 `docs/plan/`：每个计划明确唯一主题、owner、失效/归档条件；完成后的计划提炼结论后移入 `.local/`，避免读者把历史计划当当前规范。
- 为 architecture 页面增加“owner、适用版本/平台、验证命令、相关 decision”元信息；不要手抄事件/配置清单，继续使用生成脚本。
- 将本报告的高置信结论分别落到长期权威位置；报告本身不作为第二份规范。
- 把断链、目录漂移、决策字段缺失、文档预算作为 PR 必经门禁，并保留已有反向 self-test。

### 3.9 自动化、Hooks 与 CI（参考第 9 节）

**现状判断：CI DAG 基础良好，但门禁覆盖面和可诊断性有缺口。** 当前 workflow 有 `static/test/audit/coverage/all-checks-passed`，并固定 Actions SHA、使用 npm cache 和超时；`run-gates.mjs` 也有依赖 DAG。尚未见安全扫描、包内容检查、发布 dry-run、变更影响选择或测试 artifact 上传。

**具体整改：**

- 把“必过”与“仅诊断”明确分开；当前 `allowFailure` 没有实际配置时，删除死逻辑或补一个有明确 owner/失效日期的非阻断 gate，避免虚假灵活性。
- `runCommand` 的 `error` 事件只返回 false，建议至少输出命令、退出信号/错误和 gate id；不能只留下“FAIL”而没有可行动信息。
- 增加 `npm pack --dry-run`/包内容检查，确保发布包只含预期 `dist/src` 和必要元数据；增加 `npm audit --omit=dev` 或等价供应链检查，明确例外记录。
- PR CI 增加变更影响策略：文档-only 只跑 docs gate；源码/测试跑对应 build/test；发布候选跑全矩阵。若选择器复杂度大于收益，保留全跑并记录耗时预算。
- 为失败 lane 上传测试报告、coverage summary、TUI artifact 和版本信息；audit 产物必须确认不包含本地路径、prompt 或 secret。
- 保持真实 smoke 不在默认 CI 运行，另设人工触发的受保护 workflow，显示费用、凭据来源和清理步骤。
- 对 Actions 权限显式设置 `contents: read`，使用 Dependabot 更新 action SHA，并在发布 workflow 使用环境保护/人工批准。

### 3.10 安全与供应链（参考第 10 节）

**现状判断：应用边界意识较强，开源安全运营缺失。** 根规则明确不读 Codex 凭据、secret 不进入事件/artifact/report，`.gitignore` 也覆盖常见秘密；但仓库没有 `SECURITY.md`，也没有依赖审计和漏洞修复流程。

**具体整改：**

- 发布 `SECURITY.md`，写明私下报告渠道、响应 SLA、支持版本、临时缓解、修复测试和公告方式。
- 在 CI 增加依赖漏洞检查，并为不可升级漏洞建立带 CVE/影响范围/补偿控制/复评日期的 decision 或 security advisory 记录。
- 加入 secret scanning（提交和 PR）以及“artifact 内容审计”；特别检查 `.reprise`、日志、报告、TUI 快照和真实 smoke 输出。
- `npm pack --dry-run` 后对 tarball 做 allowlist 检查；发布前验证 `files` 字段、许可证、README、Node engines 和 lockfile 一致性。
- 对外部输入（路径、JSON、模型响应、子进程输出、endpoint）建立统一校验/大小/超时/协议白名单；对路径复制、junction/symlink、ACL、删除和网络发送补充威胁模型和回归测试。

### 3.11 版本、发布、回滚与事故（参考第 11 节）

**现状判断：只有 npm 生命周期的最小钩子，没有完整发布闭环。** `package.json` 的 `prepublishOnly` 只运行 `npm run check`；当前没有 CHANGELOG、发布清单、版本政策、制品签名/provenance、回滚和 postmortem 模板。

**具体整改：**

- 新增 `CHANGELOG.md`，采用 Keep a Changelog 或项目自定义的可执行格式；每个用户可见变更记录迁移、兼容性、风险和回滚提示。
- 在 `CONTRIBUTING.md` 或 `docs/release-checklist.md` 规定版本来源、Node/OS 矩阵、`npm pack --dry-run`、clean checkout 安装、smoke、文档和安全扫描。
- 发布 workflow 使用受保护环境、最小权限、npm trusted publishing/provenance（若目标 registry 支持），禁止把 API key 作为构建输入。
- 明确回滚：npm 版本撤回/弃用策略、Git tag、前一版本安装验证、数据目录兼容策略、on-disk/event 迁移的向后读取与备份。
- 新增 `docs/postmortems/` 模板；真实事故必须关联修复 commit、回归测试和持久化防御规则。

### 3.12 社区协作与维护（参考第 12 节）

**现状判断：目前更像维护者单人项目。** 没有公开贡献指南、行为准则、支持入口、响应期望、owner/继任和 issue/PR 积压治理。

**具体整改：**

- 在治理文件中声明“当前阶段/支持平台/不承诺的兼容范围”，避免 README 的验证事实被误解为永久支持承诺。
- 为 Issue 设置标签和 triage 状态：需要复现、已确认、设计中、可贡献、不会修复、已解决；每季度清理无复现和过期计划。
- 为关键组件设置 owner 和 backup；当维护者无法响应时，记录接管方式、发布权限和安全联系方式。
- 路线图只保留用户可观察目标、优先级和退出条件；实现级长计划留在内部/计划层，完成后归档。

### 3.13 持续简化、稳定性和规则生命周期（参考第 13 节）

**现状判断：有 Ponytail 约束、knip、jscpd、覆盖率和文档预算，简化意识强；但缺少周期性指标和删除触发器。**

**具体整改：**

- 每季度记录 5 个指标：本地 check 时长、CI 时长/失败率、flake 率、依赖升级积压、超长文件/重复率；只对会改变决策的指标负责。
- 为每条 gate 写 owner、成本、拦截的真实失败、例外和复评日期；连续多个周期没有捕获有效问题的 gate 进入删除评估。
- 把四个超长文件和双 Pack 重复设为复杂度预算项目；目标不是为了指标拆分，而是降低变更冲突、上下文长度和失败定位时间。
- 每次事故复盘只新增最小防御：一个测试、一个日志字段或一条门禁；避免把一次性事故变成全局复杂流程。

## 4. 关键问题清单（按可执行性排序）

### P0-A：开源协作入口缺失

- 证据：根目录和 `.github/` 未发现 CONTRIBUTING、SECURITY、CODE_OF_CONDUCT、GOVERNANCE、SUPPORT、CHANGELOG、CODEOWNERS、PR/Issue 模板。
- 风险：外部贡献者不知道如何复现、验证、报告漏洞、申请 review 或理解平台/费用边界；agent 贡献也没有统一输出契约。
- 最小修复：先新增 6 个短治理文件（可合并 SUPPORT/GOVERNANCE），一个 PR 模板、三个 Issue 模板和 CODEOWNERS；不引入机器人或审批委员会。

### P0-B：源码规模违反自身工程上限

- 证据：`src/application/experiment.ts` 约 3044 行；`src/infrastructure/recovery-tools.ts` 约 1621 行；`src/tui/controller.ts` 约 1214 行；`src/environment/local-workspace-provider.ts` 约 1014 行；根 AGENTS 明确要求源码文件不超过 1000 行、函数不超过 100 行。
- 风险：review 不可局部化，agent 需要加载过多上下文，跨职责修改易破坏持久化/取消/错误边界。
- 最小修复：先加行数/函数长度报告但不要立即把它做成硬 gate；为四文件逐一建立拆分 brief，每次只抽一个稳定职责并运行受影响测试。

### P1-C：门禁失败信息不足

- 证据：`scripts/run-gates.mjs` 的 `runCommand` 在子进程 `error` 时只返回 `false`；汇总仅打印 gate label，未记录命令、退出码/信号和环境。
- 风险：CI 失败需要人工重现，外部贡献者无法自助诊断。
- 最小修复：输出 gate id、完整命令、退出码、signal、Node/OS/git SHA；保留原始 stderr；失败 lane 上传摘要。

### P1-D：安全和发布闭环缺失

- 证据：无 SECURITY/CHANGELOG/release checklist；`prepublishOnly` 仅为 `npm run check`；CI 无依赖漏洞、包内容和 provenance 检查。
- 风险：漏洞报告无安全通道，发布可能包含错误文件或缺失迁移/回滚说明。
- 最小修复：先补文档和 dry-run 检查，再设计受保护发布 workflow；真实 Runtime 不加入默认 CI。

### P1-E：任务证据链依赖人工习惯

- 证据：存在 `development-plan.md` 和 `progress/MASTER.md`，但无 Issue/PR/task brief 模板；历史验证记录集中在进度文档，不能约束新变更。
- 风险：范围膨胀、跳过检查、决策未记录和“测试通过但未覆盖新行为”。
- 最小修复：PR 模板强制 Done-means、验证、跳过项、风险、回滚和文档/decision 状态；新增一个不依赖框架的模板完整性反向测试。

## 5. 分阶段整改路线

### 阶段 1：0–7 天，补齐最小协作闭环

1. 新增 CONTRIBUTING、SECURITY、CODE_OF_CONDUCT、SUPPORT、CHANGELOG、PR/Issue 模板、CODEOWNERS。
2. 为所有入口写准确的 Windows 11、Node 22、真实 smoke opt-in、费用和凭据说明。
3. 为 agent 输出和 PR 验证字段建立模板；将“文档-only / 源码 / 真实 smoke”的检查选择写清楚。
4. 将 `run-gates.mjs` 的子进程失败信息改为可诊断输出，并附一个失败 self-test。
5. 验证：`npm run verify:docs`；只改文档不跑 build/test。

### 阶段 2：8–30 天，降低变更风险

1. 为四个超长文件各建一个拆分 brief，先梳理职责、调用者、状态/持久化边界和受影响测试。
2. 添加依赖方向、包内容、secret/artifact 检查；每个新增 gate 都附反向用例。
3. 将测试分层并补齐关键负向控制：损坏持久化、取消/超时、路径链接/权限、外部响应、secret 泄漏。
4. 在 CI 上传可脱敏的 coverage、测试、TUI 和 gate 摘要，定义 flake 处理规则。
5. 验证：针对每个拆分只跑 `npm run build` 加直接受影响的 `dist/test`；门禁改动跑 `npm run check`。

### 阶段 3：31–90 天，发布和社区可持续

1. 建立受保护发布 workflow、版本和 changelog 规则、`npm pack --dry-run` allowlist、依赖审计和 provenance。
2. 新增 release checklist、postmortem 模板和回滚演练；记录数据目录/事件格式的兼容策略。
3. 建立 owner/backup、Issue triage、响应期望、路线图和季度积压治理。
4. 设定复杂度预算和季度简化审计，定期删除无收益的 gate、文档、兼容层和 dead code。
5. 发布前验证：clean checkout + `npm ci`、包安装、`npm run check`、受支持 OS/Node 矩阵、文档/安全扫描、回滚演练证据。

## 6. 建议的长期门禁矩阵

| 变更类型 | 本地快速检查 | PR 必过 | 人工/受保护检查 |
|---|---|---|---|
| 仅 `docs/` | `npm run verify:docs` | docs gate | 无 |
| TypeScript/测试 | build、受影响 `dist/test` | `npm run check` 或等价完整 gate | 无 |
| Schema/事件/on-disk/prompt/tool | build、受影响契约/恢复测试 | 完整 gate + decision/schema 反向测试 | 迁移/兼容人工审查 |
| Pack/外部 Runtime/凭据 | build、fixture 测试 | 完整 gate | 显式 opt-in smoke、费用/脱敏复核 |
| CI/发布/依赖 | 配置 lint、包 dry-run | 完整 gate + 安全/供应链检查 | 受保护环境批准、回滚演练 |
| TUI snapshot | 受影响测试 | `npm run audit:tui:check` | 人工查看新帧（仅必要时） |

## 7. 完成判据（整改 PR 的 Done-means）

- [ ] README、CONTRIBUTING、SECURITY、SUPPORT、CODE_OF_CONDUCT、CHANGELOG 和 `.github` 模板存在，链接无断裂。
- [ ] 至少一个 bug、feature 和 agent/task brief 示例能提供复现/范围/Done-means/验证/回滚信息。
- [ ] CODEOWNERS 覆盖核心、Pack、基础设施、TUI、CI 和架构文档。
- [ ] gate 失败输出包含可复现命令、退出信息、环境和脱敏 artifact；故意失败测试证明新 gate 确实能拦截坏输入。
- [ ] 四个超长文件各有拆分结果或有明确、短期、带 owner 和复评日期的例外记录；函数超过 100 行的热点已清单化并逐步下降。
- [ ] 发布包 allowlist、依赖漏洞扫描、secret scanning、许可证和 provenance 有明确结果或记录的例外。
- [ ] 至少一次取消/损坏持久化/路径边界/外部 JSON/secret 泄漏负向验证具备回归用例。
- [ ] 发布 checklist、回滚步骤和 postmortem 模板经一次演练。
- [ ] 本次整改的最终验证按改动范围运行，并在 PR 中列出未跑的真实 smoke 及原因。

## 8. 本次审查的验证与限制

### 已执行

```text
git status --short                 # 审查开始时干净；报告生成后出现并行工作树变更
npm run verify:docs                # 通过：self-test 4 类坏输入均被拒绝，verify-docs: ok
```

### 未执行及原因

- 未运行 `npm run build`、`npm run check`、完整测试或真实 Codex/Claude smoke：本次只新增审查文档，仓库规则明确文档改动使用 `npm run verify:docs`，且真实 smoke 会触发外部服务/费用，必须显式 opt-in。
- 未对 `.reprise*/`、`dist/`、`node_modules/` 和生成协议 schema 做逐文件语义审查；它们是本地运行数据、构建物、依赖或生成物，不应成为本次工程规范结论的主要事实源。审查期间检测到源码、测试、文档和脚本的并行变更；这些变更没有被本报告修改或归因。
- 未把“没有发现”表述为绝对不存在；治理文件结论基于当前工作树根目录和 `.github/` 的文件清单，后续新增文件应重新验证。

## 9. 结论

Reprise 不需要再堆叠一套复杂流程；应优先把已经存在的高质量内部约束变成外部可执行的协作、发布和安全闭环，并处理明显的大文件/职责集中问题。首个整改 PR 应保持小而可审查：治理入口 + PR/Issue 模板 + gate 诊断输出；第二阶段再按真实失败逐步增加安全、依赖、包内容和架构边界门禁。任何新规则都必须绑定真实失败、owner、验证方式、例外和删除/复评条件，否则宁可不加。

